.env.example
# Orderfield needs no env secrets for the kernel itself.
# Optional:
# OF_AGENT="my-agent --headless"   # generic spawn argv prefix
# OF_ADAPTER=claude                # preferred adapter when unset on CLI
# OF_JSON=1                        # machine-readable stderr events (same as of --json)
