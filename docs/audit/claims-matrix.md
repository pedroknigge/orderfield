# Documentation claims audit

Supporting docs claimed a 0.6.5 contract while lock, snapshot, and paths still described the pre-split layout.

Code wins. Inventory first. Living-claims v0: anchors, severity, verdicts.

Patch Contradicted and Partial rows. Do not invent kernel to match prose.

Zero critical Contradicted after the pass. Remaining Partials are protocol honesty (C-014/C-015/C-016), REVIEW adoption (C-080), and external multi-agent field dogfood (C-153). LEARN-002 / WAL-002 readers and writers are on the 0.7.2 line. Saturation control is on 0.7.3 (C-084). Issues #54–#57 are on 0.7.4 (C-085..C-088). Invariant evals + external brief are on 0.7.5 (C-089). Threat-model honesty + pack exclusivity evals are on 0.7.6 (C-090). Atomic close / ACTIVE / done_when lint are on 0.7.7 (C-091..C-093). Corpus recovery / stale-field / multi-harness residual are on 0.7.9 (C-094..C-096). Close/nested honesty guides + doctor skill VERSION skew are on 0.7.10 (C-097). Deep-install Codex schema argv basename is on 0.7.11 (C-098). Durable multi-day resume is on 0.7.12 (C-099). Sibling-field roster UX is on 0.7.13 (C-100). `budget.seconds` honesty is on 0.7.15 (C-101). Mid-flight amend evals are on 0.7.16 (C-102). Wave-report quality gate is on 0.7.17 (C-103). Packet sizing lint is on 0.7.18 (C-104). Threshold stop-spawn loop is on 0.7.19 (C-105). Resume after process death is on 0.7.20 (C-106). Stronger generic done_when lint is on 0.7.21 (C-107). In-flight packed-age watchdog is on 0.7.22 (C-108). Contrast report renderer is on 0.7.23 (C-109). Wave list/show is on 0.7.24 (C-110). Doctor one-pass skew is on 0.7.25 (C-111). Orphan packed-child cleanup is on 0.7.26 (C-112). Root stub refuse/migrate is on 0.7.27 (C-113). `of status --json` is on 0.7.28 (C-114). Multi-wave residual-loop evals are on 0.7.29 (C-115). Nested field lifecycle is on 0.7.30 (C-116). Mid-epic handoff packet is on 0.7.31 (C-117). Closed-field archive is on 0.7.32 (C-118). Live in-flight visibility is on 0.7.33 (C-119). `of pack --explain` is on 0.7.34 (C-120). 0.7.35 is the long-task residual-theater threat-model addendum. 0.7.36 is the long-mission operator walk. The adversarial dual-truth corpus is on 0.7.37 (C-121). Pack `--out` accepts the physical nested path on 0.7.38 (C-122). Doctor skill SKEW is advisory, residual dest is awaiting, and first-home roster label is `first` on 0.7.39 (C-123). Live `PULSE` progress under `running` is on 0.7.40 (C-124). Cross-field open-pack roster is on 0.7.41 (C-125). Multi-wave close checklist is on 0.7.42 (C-126). The mortal-install one-sitting demo is on 0.7.43 (C-127). Harness matrix residual on deep-install dests (claude/codex/cursor) is on 0.7.44 (C-128). 0.7.45 is the close-is-proof RFC invariants page (C-129). 0.7.46 is the daily ask-to-update when `of` is behind (C-130). 0.7.47 is optional per-task model hints (C-131). 0.7.48 is the efficiency signal + propose uptier/downtier (C-132). 0.7.49 is `of contrast --diff` narrative (C-133). 0.7.50 is the claims honesty gate (C-134). 0.7.51 is the README use-case opening (C-135). 0.7.52 is the README Mid-flight H2 before Install (C-136). 0.7.53 is skill-first cheap vs frontier propose (C-137). 0.7.54 is quoted skill YAML + Shared `~/.gemini/skills` (C-138). 0.7.55 is residual optional top-level `v` (C-139). 0.7.56 is ResidualQuality structured evidence over 4000 chars (C-140). 0.7.57 is grok + agy consented `--model` passthrough (C-141). 0.7.58 is the turn-end quote-PULSE leader duty via reused `InFlightSignal.speak_line` on status/resume (C-142). 0.7.59 is skill-first same-harness vs multi-harness ask before pack (C-143). 0.7.60 tightens detect to present / missing / PATH≠auth (C-009; C-015 stays Partial). 0.7.61 is the living model catalog (C-144). 0.7.62 is stream-json / JSON streams feeding the same PULSE (C-145). 0.7.63 copies agy `denied_actions` into the residual under conservative trust (C-146). 0.7.64 is agy `--json-schema` residual reuse of the Codex file with Claude omit (C-147). 0.7.65 maps `OF_TRUST=plan` onto documented native cursor/agy `--mode plan` and grok `--sandbox read-only` (C-148). 0.7.66 gates adapter resume/continue on residual `session_id` (C-149). 0.7.67 splits `SKILL.md` into a short always-loaded core plus `references/skill-appendix.md` (C-150). 0.7.68 is mechanical anti-done-theater: claim shipped only after contrast RESOLVED + residual empty (`CloseChecklist.speak_line`; C-151). 0.7.69 is the Claude `-p` + stream-json `--verbose` argv fix (#131; C-145; no new C-ID, honesty cap). 0.7.70 is packed-only honesty: status/resume `spawned` + ALIVE/QUIET/STALE from spawn metadata or scratch (`SpawnRecord`; C-152). 0.7.71 is grok documented `--output-format streaming-json` plus spawn-metadata finalize on exit/timeout/missing binary (#133; C-145; no new C-ID, honesty cap). 0.7.72 is `of issue --body-file` naming `.orderfield/work/scratch/<child_id>/` on refuse plus the leader HITL draft path (#134; no new C-ID, honesty cap). 0.7.73 is Codex `residual.codex.schema.json` `usage.type` unique `["object","null"]` (#135; C-027; no new C-ID, honesty cap). 0.7.74 is doctor closed-field historical packs informational (C-111; #137; no new C-ID, honesty cap). 0.7.75 is the conservative collect diagnostic honesty cut (#138; C-146; no new C-ID). 0.7.76 is `integrate --partial` hold reason naming `skipped_in_flight` instead of `wave closed` while siblings fly (#139; no new C-ID, honesty cap). 0.7.77 is `of learn` length advisory like `pack --slice` (C-056; #146; no new C-ID, honesty cap). 0.7.78 is webhook HMAC + replay as a contrast PAIR gate (C-047; no new C-ID, honesty cap). 0.7.79 is skill appendix **Production mode** + Gate A before features (C-150; no new C-ID, honesty cap). 0.7.80 is timeout / idempotency / health as VERIFIED_CONTRACT (`ContractSurface`; C-047; no new C-ID, honesty cap). 0.7.81 is done residual close evidence requiring artifact SHA + rollback command (`CloseEvidence`; C-033 / C-051; no new C-ID, honesty cap). 0.7.82 is the living map checklist → of contrast / of close / residual (`LivingMap`; C-126 / C-151 / C-047; no new C-ID, honesty cap). 0.7.83 is the multi-harness mix playbook (roles on one harness vs mix; claude/codex/cursor/grok/agy; C-143; no new C-ID, honesty cap). 0.7.84 is long-task efficiency mix from honest session/balance signals (`AdapterBalance`; unknown if unpublished; C-132 / C-143; no new C-ID, honesty cap). 0.7.85 is the fresh-context evaluator packet consent path (adversary/verifier; ask first; not a close gate; C-080 stays Partial; no new C-ID). 0.7.86 is Orca worker stop/release duty plus leftover of-worktree doctor WARN (C-045; no new C-ID, honesty cap). 0.7.87 tightens C-151: claiming shipped also requires quoting the printed `CloseChecklist.speak_line` (honesty cap; no new C-ID). 0.7.88 extends C-047: `/version` or a release header is VERIFIED_CONTRACT (`ContractSurface`; extract VERSION; `--surface internal` cannot hide; honesty cap; no new C-ID). 0.7.89 extends C-093 / C-150: Prod§15 day-90 runbook path in `done_when` before close (`RunbookPath`; production/day-90 cue only; honesty cap; no new C-ID). 0.7.90 extends C-045: native Codex spawn honors a recorded child worktree (`-C` + field/Git `--add-dir`) or refuses before launch (`CodexWorktree`; honesty cap; no new C-ID). 0.7.91 extends C-119 / C-152: a started-only re-spawn dominates a leftover residual so status cannot hide flight (`SpawnRecord.flying`; honesty cap; no new C-ID). 0.7.92 extends C-083: a successful `of phase` refreshes the covering digest (`PhaseDigest`) so `of next-wave` does not require `--recompute` (#164; honesty cap; no new C-ID). 0.7.93 is generic `OF_AGENT` shell-quoted argv (`GenericAgent` / `shlex.split`) plus dry-run `shlex.join` so a space path stays one token (#165; honesty cap; no new C-ID). 0.7.94 extends C-034: an empty wave (zero `packed_children`) skips the phase integrate requirement so `of phase` does not need `--force` (#166; honesty cap; no new C-ID). 0.7.95 extends C-034 / C-035: resume names `INTEGRATE --RECOMPUTE` on digest drift and `IntegrationDigest` omits spawn-owned residual keys (#168; honesty cap; no new C-ID). 0.7.96 extends C-135: authority-axis README hero, shortest install → first close, and an explicit planning-with-files contrast (same disk-plan category, different authority-kernel product; honesty cap; no new C-ID). 0.7.97 tightens `PackagingBump`: one VERSION / GitHub tag per proven invariant; docs-only and missing-Proof current cuts fail (honesty cap; no new C-ID). 0.7.98 is identity-stale + flying `UNPACK --FORCE` plus the spec rev-stale note (`PacketRevStale`; #178; honesty cap; no new C-ID). 0.7.99 is successful close as terminal (`CloseProof` / `ActiveField.release_closed`; not ACTIVE / pulse not ALIVE / `spawn_blocked` cleared; `recovery/post-close-terminal`; #180; honesty cap; no new C-ID). 0.7.100 is doctor / close WARN on audit OVER or fat scratch (`AuditPressure`; #181; C-084 / C-111; honesty cap; no new C-ID). 0.7.101 is doctor leftover-root `migrate required` plus open-sibling CLOSE hygiene (C-111; #182; honesty cap; no new C-ID). 0.8.1 is cited plan-doc sync (`PlanDocSync`; #188; C-111; honesty cap; no new C-ID). 0.8.2 is after-integrate same-turn `next` (`DriveAfterIntegrate`; #191; C-025; honesty cap; no new C-ID). 0.8.3 is `of issue` create HITL lock (`IssueConfirm`; `--confirm` or TTY yes; dry-run is not HITL; #193; honesty cap; no new C-ID). 0.8.4 is `yolo` + `inherit` as audited operator actions (`OperatorAction`; C-066 / C-067; honesty cap; no new C-ID). 0.8.5 is `of issue --search` listing open issues via Issues list API + local filter (`IssueList`; #198; honesty cap; no new C-ID). 0.8.6 is Cursor `--model-tier` without `--model` refusing pack/spawn (`AdapterHints.TIER_NEED_MODEL`; C-131; #199; honesty cap; no new C-ID). 0.8.7 is ended spawn without a schema-valid residual as `done_without_residual` not ok/ALIVE, and host Write denials not `escalate_up` (`HostWriteDenial`; C-028 / C-152; #200; honesty cap; no new C-ID). 0.8.8 is `of issue --body-file` accepting leader `ISSUE.md` / `ISSUE-*.md` under `.orderfield/work/scratch/leader/` (`_issue_scratch_rel_ok`; #201; honesty cap; no new C-ID). 0.8.9 is resume/status `next` INTEGRATE after a successful `of collect` (`CollectReady`; #204; honesty cap; no new C-ID). 0.8.10 is `of gc` / `of retain` importing `PHASES` so a stored learning with `phase` does not NameError (`#205`; honesty cap; no new C-ID). 0.8.4 is `yolo` + `inherit` as audited operator actions (`OperatorAction`; C-066 / C-067; honesty cap; no new C-ID). 0.7.8 is the published-voice packaging line. Duplicate C-065 retired (shim is C-081). Duplicate CLI handler copies in `ops.py` are gone. A cut, a resume, a different model — the matrix still points at code. The results do not have to change.0.7.75 corrects the conservative collect diagnostic without a new C-ID: C-146 now covers pending/unavailable wording, known spawn facts, adapter-tied permission possibility, and supported conservative delivery (#138).
> Hub: [AGENTS.md](../../AGENTS.md)
> **Code is source of truth.** Docs do not override implementation.
> **Living claims v0:** anchors + severity.

**Date:** 2026-09-08
**Scope:** project
**Intent:** audit → integrate (patch supporting docs)
**Out:** root
**Auditor:** documentation-manager
**Code rev:** VERSION `0.8.11`

## Summary

| Verdict | Count |
|---------|------:|
| OK | 147 |
| Partial | 5 |
| Missing | 0 |
| Contradicted | 0 |
| Unverifiable | 1 |

| Severity | Count |
|----------------:|
| critical | 126 |
| normal | 27 |

**Truth score (advisory):** `(147*100 + 5*50) / 153 = 97.7` (153 matrix rows; unique IDs C-001…C-153)
**CI gate:** no critical Contradicted; advertised truth score matches the table and stays ≤98%; marketing theater on SKILL / `/of` / README fails. `python3 docs/audit/check-claims.py` is wired into `validate-skill.sh`. Local `scripts/audit-claims.sh` is not in this repo.

**Top risks (post-patch):**
1. Same-harness is the default; skill asks before pack (C-143); no `of ask` CLI — Partial by design (C-014).
2. `detect` / `doctor` PATH ≠ credentials or session authority — documented Partial (C-015). Worktree/process bounds are honesty surfaces, not a security guarantee (C-016).
3. Role/product-workspace compliance and metric truth remain contract; the field lock covers `MUTATING_COMMANDS` only, not spawn/handoff/gc/learn/worktree. `spec` and `checkpoint` joined the lock in 0.6.7.
4. Token/local-budget/inherited-depth accounting and `scale_up` are **reserved** (no telemetry).
5. LEARN-002 spawn pid/starttime registry plus unauthenticated provenance is on the 0.7.1 line (C-070). Not OS-user authentication.
6. WAL-002 writer rematerialize is on the 0.7.2 line (C-071). Immediate checkpoint after `OF_WAL_CRASH=after-current` keeps `children_spawned=2` and packets e1/e2 (`tests/test_field_wal.py`).
7. Branch protection `required_approving_review_count >= 1` is restored; independent review in merge history is unproven (PR #40 and 0.7.0 #41/#42/#43/#45 merged with empty reviews after a temporary review-requirement window) (C-080 Partial).
8. #48 leftover-canonical residual uses `packet_residual_file` for collect/integrate/unpack/complete-stale (C-082). `tests/test_sibling_residual.py` covers the leftover path.
9. A disobedient leader can still write product files without pack (kernel does not lock product).

**Recommended next Intent:** C-153 stays Partial until a checkable external field artifact is cited — do not invent case studies. REVIEW-001 stays Partial until an independent review exists in merge history. Out-of-scope auditor items: [out-of-scope.md](out-of-scope.md). Do not bump protocol claims.

## Code inventory (high level)

| Kind | Evidence | Notes |
|------|----------|-------|
| Packages / apps | single repo, no workspace / `pyproject.toml` | **Stack: python** (stdlib CLI). **Monorepo: no.** No ArkGate. |
| Kernel CLI | `scripts/of.py` | Shim: `from of.cli import main`. Public cmds: init, status, resume, pulse, checkpoint, detect, doctor, retain, gc, learn, migrate, worktree, wave, validate, pack, unpack, render, handoff, spawn, collect, integrate, phase, patch, next-wave, spec, spec-diff, contrast, close, eval |
| Kernel internals | `scripts/of/{field,wal,learn,retain,spec,pack,regime}.py` + `scripts/of/cli/` | Groups: `init_cmd` / `ops` / `wave` / `field_cmd` / `spec_cmd`. Parser + lock wrapper in `cli/__init__.py`. `field` re-exports public WAL/learn/retain names (`FieldWal` / `FieldLearnings` / `FieldRetain`). SCOPE-GODSPLIT shipped ([PR #59](https://github.com/pedroknigge/orderfield/pull/59)/[#103](https://github.com/pedroknigge/orderfield/pull/103)); remaining `field.py` is field I/O. |
| `MUTATING_COMMANDS` | `scripts/of/field.py` | Exact set: `init`, `new`, `pack`, `unpack`, `collect`, `integrate`, `phase`, `patch`, `next-wave`, `migrate`, `spec`, `checkpoint`, `close`. Sole `with field_lock` is `of.cli.main` |
| Adapters | `scripts/of_adapters.py` | `ADAPTER_ORDER` = claude, codex, cursor, opencode, orca, grok, agy, qwen, generic. `INLINE_CONTRACT_ADAPTERS` = orca, generic |
| Schemas | `schemas/*.json` | order, state, packet, residual, residual.codex, wave-report, session, **learning**, **requirements** |
| Install | `install.sh` | harness dests + installed-kernel `of`; copies `scripts/of/` with the skill tree |
| Tests | `tests/test_kernel.py`, `tests/test_kernel_{field,spec,pack,regime,cli,origin}.py`, `tests/test_packaging.py` | kernel split by invariant class + packaging |
| CI | `.github/workflows/test.yml` | unittest + `of eval --strict --kernel` + `validate-skill.sh`; gitleaks; ubuntu/macos × 3.11/3.13 |
| Doctrine | `SLAVE.md`, `references/principles.md`, `references/adapters.md` | |

## Claims matrix

| ID | Claim (quote or paraphrase) | Source doc | Code evidence | Anchor path | Anchor symbol | Anchor hash | Severity | Verdict | Action |
|----|----------------------------|------------|---------------|-------------|---------------|-------------|----------|---------|--------|
| C-001 | Native adapters include `agy` | SKILL / README / adapters | `ADAPTER_ORDER` includes `agy` | `scripts/of_adapters.py` | `ADAPTER_ORDER` | — | critical | OK | keep |
| C-002 | Flags before `-p` for agy | adapters.md | `build_spawn_argv` agy branch | `scripts/of_adapters.py` | `build_spawn_argv` | — | critical | OK | keep |
| C-003 | Phase-prefix `done_when` | SKILL / README / CHANGELOG | `done_when_for`, `done_when_closed_phases` | `scripts/of/regime.py` | `done_when_for` | — | critical | OK | keep |
| C-004 | `--requires-tool` on pack; spawn refuses | SKILL / adapters / CHANGELOG | pack argparse + `missing_tools` | `scripts/of/cli/` / `scripts/of_adapters.py` | `missing_tools` | — | critical | OK | keep |
| C-005 | Reference-load SLAVE (abs path); orca/generic may inline | SKILL / README / CHANGELOG | `render_prompt`, `INLINE_CONTRACT_ADAPTERS` | `scripts/of/pack.py` | `render_prompt` | — | critical | OK | keep |
| C-006 | Grok headless `-p`; `--always-approve` only under `OF_TRUST=yolo` | adapters / CHANGELOG | grok argv + `YOLO_FLAGS` | `scripts/of_adapters.py` | `build_spawn_argv` / `trust_flags` | — | critical | OK | patched; 0.6.7 trust |
| C-007 | Codex `exec`; `--dangerously-bypass-approvals-and-sandbox` only under `OF_TRUST=yolo` (never `--full-auto`) | adapters / CHANGELOG | codex argv + `YOLO_FLAGS` | `scripts/of_adapters.py` | `build_spawn_argv` / `trust_flags` | — | critical | OK | patched; 0.6.7 trust |
| C-008 | `install.sh` symlinks `of` to **installed** skill copy | CHANGELOG / adapters | `of_bin_dirs` / link to dest `scripts/of.py` | `install.sh` | | — | critical | OK | keep |
| C-009 | Detect lists harness CLIs on PATH as present/missing plus `honesty: PATH≠auth (Partial)`; never `auth=ok` | README / adapters / SKILL | `AdapterDetect` / `cmd_detect` / doctor adapters | `scripts/of_adapters.py` / `scripts/of/cli/ops.py` | `AdapterDetect` / `cmd_detect` | — | normal | OK | 0.7.60 |
| C-010 | Pack is cap surface (`max_children`, `spawn_blocked`) | SKILL | `cmd_pack` / `spawn_is_blocked` | `scripts/of/cli/wave.py` / `scripts/of/pack.py` | `cmd_pack` | — | critical | OK | keep |
| C-011 | PATH via install → installed skill `of` | README.md | `install.sh` of symlink | `install.sh` | | — | critical | OK | keep |
| C-012 | Cursor prompt is reference-load | adapters.md | `render_prompt` | `scripts/of/pack.py` | `render_prompt` | — | critical | OK | keep |
| C-013 | SLAVE reference-load by default | SKILL.md | `render_prompt` / `--inline` | `scripts/of/pack.py` | `render_prompt` | — | critical | OK | keep |
| C-014 | Leader asks same-harness vs multi; uses detect inventory | SKILL.md / AGENTS.md | protocol + `cmd_detect`; no `of ask` | `scripts/of/cli/ops.py` | `cmd_detect` | — | normal | Partial | keep protocol (C-143 skill ask); optional `of ask` CLI later |
| C-015 | `detect` proves auth / “logueado” | user phrasing | PATH binary presence only; `AdapterDetect` prints `auth=not-verified` and `honesty: PATH≠auth (Partial)`; doctor/SKILL/README teach not credentials or session authority | `scripts/of_adapters.py` / `scripts/of/cli/ops.py` | `AdapterDetect` / `cmd_detect` | — | normal | Partial | PATH ≠ credentials/session authority; pair C-016 |
| C-016 | Worktree isolation always enforced by kernel | SKILL isolation notes | workspace is documentation only; `of worktree` is an opt-in helper; worktree/process bounds are honesty surfaces, not a security guarantee | `scripts/of/field.py` | `default_order` | — | normal | Partial | honesty surface, not a jail; pair C-015 |
| C-017 | Skill beats child | SKILL / principles | procedure only | `references/principles.md` | | — | normal | Unverifiable | keep |
| C-018 | `--done-when` scopes to current phase; `--done-when-mission` edits untagged mission list | SKILL / README / CHANGELOG / AGENTS | `cmd_patch`, `mission_done_when`, `phase_done_when` | `scripts/of/cli/field_cmd.py` / `scripts/of/regime.py` | `cmd_patch` | — | critical | OK | keep |
| C-019 | Cut optional when owners obvious; pays vs theater doctrine | SKILL / README / principles | leader protocol (no new regime) | `SKILL.md` | §2 | — | normal | OK | keep doctrine |
| C-020 | `of resume` reconstructs in-flight from disk; recovery brief; auto_continue; no auto-spawn | SKILL / README / CHANGELOG / architecture | `cmd_resume`; `resume_auto_continue_lines` | `scripts/of/cli/ops.py` | `cmd_resume` | — | critical | OK | keep; multi-day 0.7.12 is C-099 |
| C-021 | `of checkpoint --summary` optional one-screen leader narrative; refuse huge dumps | SKILL / README / CHANGELOG | `cmd_checkpoint`; `CHECKPOINT_MAX_CHARS` | `scripts/of/cli/ops.py` / `scripts/of/field.py` | `cmd_checkpoint` | — | normal | OK | keep |
| C-022 | Auto snapshot `.orderfield/session.json` facts on pack/spawn/collect/integrate/patch/phase/next-wave **and** unpack/spec/close/gc/learn/migrate/checkpoint | README / architecture / SKILL / kernel feature | `snapshot_session` call sites | `scripts/of/cli/` | `snapshot_session` | — | critical | OK | patched docs to name the full set |
| C-023 | `of status` surfaces in-flight; render/handoff continuation note when scratch nonempty | SKILL / README / adapters | `cmd_status`; `render_prompt` continuation | `scripts/of/cli/ops.py` / `scripts/of/pack.py` | `cmd_status` / `render_prompt` | — | critical | OK | keep |
| C-024 | `session.json` forbidden to slaves like `state.json` | SKILL / SLAVE / AGENTS | `SESSION_FORBIDDEN` in `default_order` | `scripts/of/field.py` | `default_order` | — | normal | OK | keep |
| C-025 | Leader step 0 = `of resume` when ORDER exists; open field auto-continues; after collect+integrate, idle + actionable `next` is same-turn work (`DriveAfterIntegrate`; report is not a stop; ordinary next-wave is not a consent ask); clone/checkout of an open `.orderfield/` plus installed skill is the same auto-continue (operator risk, not an escape; rule 0 stays) | SKILL.md §0 / AGENTS rule 0 / SLAVE.md | leader/slave protocol; `DriveAfterIntegrate`; `SkillDriveAfterIntegrate`; `SkillCheckoutAutoContinueHonesty`; `recovery/drive-after-integrate` | `SKILL.md` / `AGENTS.md` / `scripts/of/cli/ops.py` | §0 / rule 0 / `DriveAfterIntegrate` | — | normal | OK | keep doctrine; 0.8.2 #191; checkout honesty |
| C-026 | Residual metric types/ranges are rejected before regime selection | schema / CHANGELOG / architecture | `validate_residual` | `scripts/of/pack.py` / `tests/test_kernel_regime.py` | `validate_residual` | — | critical | OK | keep |
| C-027 | Codex routes output to a separate strict-compatible residual schema (type unions unique; `usage` is `[object,null]`) | adapters / CHANGELOG | `build_spawn_argv` selects derivative; `CodexStrictSchema`; `ResidualSchemaContracts` | `scripts/of_adapters.py` / `schemas/residual.codex.schema.json` / `tests/test_kernel_regime.py` | `build_spawn_argv` | — | critical | OK | 0.7.73 #135 unique type unions |
| C-028 | Pulse is an mtime activity heuristic, not process health; does not mutate ORDER/state/session/wave; ALIVE requires an open spawn (no `ended_at`); leftover PULSE after a settled spawn is `done_without_residual` | README / SKILL / architecture | `cmd_pulse` / `maybe_notify_update` / `child_pulse_verdict` | `scripts/of/cli/ops.py` / `scripts/of/field.py` | `cmd_pulse` | — | normal | OK | keep boundary; 0.8.7 #200 |
| C-029 | Preferred package discovery exposes `orderfield` and `of` | README / PUBLISH | alias skill + packaging test | `of/SKILL.md` / `tests/test_packaging.py` | `RepositoryAliasSkill` | — | critical | OK | keep |
| C-030 | Generated JSON matches public schemas; runtime validation uses the same contract | architecture / kernel feature | `validate_public_schema` | `scripts/of/field.py` / `schemas/` | `validate_public_schema` | — | critical | OK | keep; schemas include learning + requirements |
| C-031 | `MUTATING_COMMANDS` take `.orderfield/field.lock` in `of.cli.main`; JSON replacement is atomic via `dump_json` | README / SKILL / principles / architecture | `MUTATING_COMMANDS`, `field_lock`, `dump_json` | `scripts/of/field.py` / `scripts/of/cli/__init__.py` | `MUTATING_COMMANDS` / `main` | — | critical | OK | patched: lock set is **not** spawn/handoff/gc/learn/worktree; 0.6.7 added spec/checkpoint |
| C-032 | New packet execution is bound to canonical live identity, revision, content, and nonsymlink paths | README / SKILL / architecture / troubleshooting | `require_registered_packet` | `scripts/of/pack.py` / `tests/test_kernel_pack.py` | `require_registered_packet` | — | critical | OK | keep |
| C-033 | Residual identity must match its packet; workspace escalates; done result_ref exists under project; done close evidence names artifact SHA of that file plus a rollback command (`CloseEvidence`) | SKILL / kernel feature / troubleshooting | `validate_residual_for_packet` / `CloseEvidence` | `scripts/of/pack.py` / `scripts/of/regime.py` | `validate_residual_for_packet` / `CloseEvidenceGate` | — | critical | OK | keep; CloseEvidence 0.7.81 |
| C-034 | Phase/wave transitions require closure, no in-flight child, complete current digest when packets exist; an empty wave has nothing to integrate so `of phase` does not require a report; phase force is audited. Resume / status print `INTEGRATE --RECOMPUTE` when a report exists but the covering digest drifted (#168) | README / SKILL / principles / architecture | `phase_transition_errors` / `wave_transition_errors` / `packed_children` / `next_legal_action` | `scripts/of/regime.py` / `scripts/of/field.py` / `scripts/of/cli/ops.py` | `phase_transition_errors` / `next_legal_action` | — | critical | OK | empty-wave #166; resume #168 |
| C-035 | Identical integration replay is a no-op/state repair; changed inputs require `--recompute`. Spawn-owned residual keys (`session_id`, `denied_actions`) are omitted from the digest (`IntegrationDigest`; #168) | README / SKILL / architecture / troubleshooting | `integration_input_digest` / `IntegrationDigest` | `scripts/of/regime.py` | `integration_input_digest` / `IntegrationDigest` | — | critical | OK | #168 spawn-owned omit |
| C-036 | Pulse child verdict ignores shared-repo product writes as evidence | README / architecture / kernel feature | verdict from packet + scratch only | `scripts/of/cli/ops.py` / `tests/test_kernel_field.py` | `pulse_once` | — | normal | OK | keep |
| C-037 | Token/local-budget/inherited-depth accounting and scale_up selection are reserved | README / SKILL / principles / architecture | `RUNTIME_OWNERSHIP`; remapped to hold | `scripts/of/regime.py` | `RUNTIME_OWNERSHIP` / `decide_regime` | — | normal | OK | keep reserved |
| C-038 | Literal `./install.sh --project` avoids recursive source copy and writes a resolving installed-kernel symlink | README / CHANGELOG | packaging regression | `install.sh` / `tests/test_packaging.py` | `InstallScript` | — | critical | OK | keep |
| C-039 | Native `qwen` adapter uses Qwen-owned positional headless argv, conservative `--approval-mode default` | adapters.md / SKILL | `ADAPTER_ORDER` includes `qwen`; `TRUST_PROFILES` | `scripts/of_adapters.py` / `schemas/order.schema.json` | `build_spawn_argv` | — | critical | OK | keep |
| C-040 | `of doctor` reports prereqs, adapter PATH/version, writable field, schemas, lock; PATH ≠ auth/ready | README / troubleshooting / kernel feature | `cmd_doctor` prints `auth=not-verified` | `scripts/of/cli/ops.py` / `tests/test_kernel_cli.py` | `cmd_doctor` | — | critical | OK | keep |
| C-041 | Episodic retention keeps useful residuals and protocol learnings; drop/dump permanently unlinks selected artifacts (operator-owned backup). WAL crash consistency is not a restorable dump | troubleshooting / kernel feature | `cmd_retain` / `cmd_gc` / `plan_field_retention` / `apply_field_retention` / `_safe_unlink` (`Path.unlink` / `rmtree`). Action name `dump` is unlink, not an export. Walks every field home as of 0.7.3 (C-084). | `scripts/of/cli/ops.py` / `scripts/of/retain.py` / `scripts/of/field.py` | `cmd_gc` / `_safe_unlink` | — | critical | OK | RETAIN-001 |
| C-042 | Fully stale wave after a leader patch is recoverable with `next-wave` without hand-editing ORDER | troubleshooting / SKILL | `packets_all_stale` / `wave_transition_errors`. Complete-stale collect/integrate uses `packet_residual_file` (C-082). | `scripts/of/pack.py` / `scripts/of/regime.py` | `wave_transition_errors` | — | critical | OK | keep; leftover path → C-082 |
| C-043 | Spawn argv previews and logs redact secrets and escalated approval material | troubleshooting / kernel feature | `argv_preview` / `redact_text` / `ArgvRedact` | `scripts/of/field.py` / `tests/test_kernel_cli.py` | `argv_preview` / `ArgvRedact` | — | critical | OK | keep; path basename 0.7.11 |
| C-044 | Versioned migrations upgrade pre-0.4.2 packets/state; protocol keys stay frozen | troubleshooting / architecture / SLAVE.md | `MIGRATION_CATALOG` / `cmd_migrate` | `scripts/of/field.py` / `scripts/of/cli/ops.py` | `cmd_migrate` | — | critical | OK | keep |
| C-045 | Optional worktree helper is opt-in and is not a process manager; native Codex spawn honors the packet child's recorded worktree with `-C`, field-home `--add-dir`, and exact Git-common-dir `--add-dir`, refusing invalid records before launch; Orca interactive `worker-start` must pair `worker-stop` then `worker-release` after collect/abandon (retain only if asked); `of worktree remove --child-id` when add was used; `worker-stop` does not delete worktrees or Host panes; Orca Host teardown is `orca worktree rm` / `terminal close --tab` (current `orca` CLI; prefer `--worktree current`); `of doctor` / close WARN when a recorded of-worktree is orphaned vs a settled child (advisory, not FAIL, no Orca process poll); collect/unpack/close print `next of worktree remove` when a record remains; SKILL / `/of` / appendix / adapters / SLAVE teach the duty; spawn does not create/remove worktrees or call Orca worker-stop / worktree rm | troubleshooting / SKILL / alias / appendix / adapters / SLAVE / kernel feature / events | `cmd_worktree`; `CodexWorktree`; `DoctorSkew.orphaned_worktrees` | `scripts/of_adapters.py` / `scripts/of/cli/wave.py` / `scripts/of/cli/ops.py` / `scripts/of/field.py` / `tests/test_spawn_trust.py` / `tests/test_kernel_field.py` / `tests/test_kernel_cli.py` / `tests/test_packaging.py` | `CodexRecordedWorktree` / `SkillCodexWorktreeSpawn` / `cmd_worktree` / `DoctorSkew.orphaned_worktrees` | — | normal | OK | 0.7.86; Codex bind 0.7.90; Host teardown 0.8.11 |
| C-046 | Runtime ownership is encoded as reserve/remove; no fake telemetry | architecture / principles | `RUNTIME_OWNERSHIP` / `RESERVED_REGIMES` | `scripts/of/regime.py` | `RUNTIME_OWNERSHIP` | — | critical | OK | keep; location is `regime.py` not `scripts/of.py` |
| C-047 | SPEC.md is the current brief; product-root prompt.md discarded; contrast is a close gate; webhook HMAC + replay is PAIR (`WebhookPair` + `--both-sides`); timeout / idempotency / health / version are VERIFIED_CONTRACT (`ContractSurface`) | SKILL / architecture / SLAVE / README | `write_spec` / `requirement_close_ok` / `WebhookPair` / `ContractSurface` / `cmd_contrast` / `cmd_close` | `scripts/of/spec.py` / `scripts/of/cli/spec_cmd.py` | `WebhookPair` / `ContractSurface` / `cmd_spec` | — | critical | OK | keep; webhook PAIR 0.7.78; ContractSurface 0.7.80; version 0.7.88 |
| C-048 | `of pack` without `--owns-requirement` is refused while binding IDs are unowned | README / SKILL / troubleshooting | `cmd_pack` dies on unowned when packet owns none | `scripts/of/cli/wave.py` / `tests/test_kernel_spec.py` | `cmd_pack` | — | critical | OK | keep |
| C-049 | Extract joins backslash-continued CLI lines | CHANGELOG / kernel feature / troubleshooting | `join_continued_lines` | `scripts/of/spec.py` / `tests/test_kernel_spec.py` | `join_continued_lines` | — | critical | OK | keep |
| C-050 | Same-wave `--owns-path` overlap dies; second implementer needs `--owns-path`; not a file lock | SKILL / SLAVE / README / principles | `cmd_pack` overlap + `copy_workspace_with_owns` | `scripts/of/cli/wave.py` / `scripts/of/pack.py` | `cmd_pack` | — | critical | OK | keep |
| C-051 | Verifier `done` requires identifying evidence + nonempty `result_ref`; any `status=done` also needs artifact SHA + rollback command (`CloseEvidence`); `phase --force` to deliver still runs SPEC close | SKILL / SLAVE / troubleshooting | `verifier_done_errors` / `CloseEvidence` / `phase_deliver_errors` | `scripts/of/pack.py` / `scripts/of/regime.py` | `VerifierEvidence` / `CloseEvidenceGate` | — | critical | OK | keep; CloseEvidence 0.7.81 |
| C-052 | REQUIREMENTS is an index over SPEC; contrast cites `SPEC.md:N`; extract precision over recall | SKILL / principles / CHANGELOG | `extract_requirements_from_spec` | `scripts/of/spec.py` / `tests/test_kernel_spec.py` | `SemanticExtract` | — | critical | OK | keep |
| C-053 | Kernel internals split into field/spec/pack/regime/cli; public CLI stays; protocol unchanged vs 0.5.7 | architecture | package `scripts/of/` + shim entry | `scripts/of.py` / `scripts/of/` | `main` | — | critical | OK | keep; 0.6 form |
| C-054 | A deictic go-ahead is not a lossless brief; kernel prints advisory note and still writes SPEC | SKILL / context-control / principles 17 | `looks_like_deictic_brief` / `warn_if_deictic_brief` | `scripts/of/spec.py` / `scripts/of/cli/` | `looks_like_deictic_brief` | — | normal | OK | keep advisory |
| C-055 | CLI commands live in `scripts/of/cli/` groups; parser + dispatch in `cli/__init__.py`; public `of` unchanged | architecture / kernel feature / CHANGELOG | `from of.cli import main`; dispatch imports `cmd_spec` from `spec_cmd`; `ops.py` has no leftover `cmd_spec`/`cmd_contrast`/`cmd_close` | `scripts/of/cli/` / `scripts/of.py` | `main` | — | critical | OK | leftover copies gone on 0.6.9 / `17709e5` |
| C-056 | `of learn TEXT` writes a field note (default), `--protocol` / `--promote` write protocol; provenance required on load; resume lists both; child prompts ≤8 protocol lines; `gc` never drops protocol; length over 400 chars is advisory (store + note); `LEARNING_MAX_LINES` is the hard dump bound | SKILL / README / kernel feature | `cmd_learn` / `save_learning` / `LearningLint` / `protocol_learning_lines` | `scripts/of/learn.py` / `scripts/of/field.py` / `scripts/of/cli/ops.py` / `schemas/learning.schema.json` | `cmd_learn` | — | critical | OK | keep; learn module re-exported by field; length advisory 0.7.77 |
| C-057 | Optional `ORDER.origin` provenance stamp; spawn/`pick_adapter` ignore origin; kernel does not fetch transcripts | SKILL / README / context-control / CHANGELOG | `origin` on order schema; `format_origin_line`; `pick_adapter` has no origin param | `schemas/order.schema.json` / `scripts/of/field.py` / `scripts/of/cli/` / `tests/test_kernel_origin.py` | `cmd_init` / `format_origin_line` | — | critical | OK | keep; 0.6.5 |
| C-058 | Field lock set is exactly `MUTATING_COMMANDS` = init, new, pack, unpack, collect, integrate, phase, patch, next-wave, migrate, spec, checkpoint, close | architecture / README / principles (was overclaiming spawn/handoff/gc/learn/worktree) | `MUTATING_COMMANDS`; single `with field_lock` in `main` | `scripts/of/field.py` / `scripts/of/cli/__init__.py` | `MUTATING_COMMANDS` | — | critical | OK | patched supporting docs; 0.6.6 added `new`; 0.6.7 added `spec`/`checkpoint` |
| C-065 | Sibling fields: `of new` / `of fields` / `--field` / `OF_FIELD`; resume roster exit 2; foreign origin gate; cross-field in-flight owns-path overlap dies | SKILL / README / glossary / CHANGELOG | `cmd_new` / `bind_active_field` / `cross_field_owns_path_conflict` | `scripts/of/field.py` / `scripts/of/cli/` / `tests/test_kernel_fields.py` | `SiblingFields` | — | critical | OK | keep; 0.6.6; roster UX 0.7.13 is C-100 |
| C-059 | `emit_event` lives in `scripts/of/field.py`, re-exported by `of`; not in the `scripts/of.py` shim | events.md | `def emit_event` | `scripts/of/field.py` | `emit_event` | — | normal | OK | patched events.md |
| C-060 | `RUNTIME_OWNERSHIP` / `RESERVED_REGIMES` live in `scripts/of/regime.py` | roadmap (was `scripts/of.py`) | module location | `scripts/of/regime.py` | `RUNTIME_OWNERSHIP` | — | normal | OK | patched roadmap |
| C-061 | Public schemas include `learning.schema.json` and `requirements.schema.json` besides order/state/packet/residual/session/wave-report | architecture / kernel feature | `schemas/` listing | `schemas/` | | — | normal | OK | patched inventory + coverage |
| C-062 | Hub docs table lists glossary, events, context-control, demo, evals, PRINCIPLES.md, `/of` alias | AGENTS.md | files exist | `docs/` / `of/SKILL.md` / `PRINCIPLES.md` | | — | normal | OK | patched AGENTS.md |
| C-063 | Architecture Compared-to link resolves to README `#compared-to` | architecture.md | heading `## Compared-to` | `README.md` | | — | normal | OK | patched fragment |
| C-064 | CI matrix is unittest + `of eval --strict --kernel` + `validate-skill.sh` + unused-imports + gitleaks on ubuntu/macos × 3.11/3.13 | CONTRIBUTING / README / evals | `.github/workflows/test.yml` jobs `test`, `unused-imports`, `gitleaks` | `.github/workflows/test.yml` | | — | critical | OK | keep; 0.7.1 added unused-imports |
| C-066 | `OF_TRUST` is authoritative for every adapter; conservative default emits no bypass; only `yolo` (alias `escalated`) emits `YOLO_FLAGS`; unknown profiles die; `yolo` is an audited operator action (`OperatorAction`; spawn speaks + records; not a silent default) | SKILL / README / adapters / CHANGELOG | `resolve_trust_profile` / `trust_flags` / `YOLO_FLAGS` / `OperatorAction` | `scripts/of_adapters.py` / `tests/test_spawn_trust.py` | `trust_flags` / `OperatorAction` | — | critical | OK | keep; 0.6.7; operator-action 0.8.4 |
| C-067 | Spawn env is an allowlist, not parent inherit; `OF_SPAWN_ENV` extends or `inherit` opts out as an audited operator action (`OperatorAction`; spawn speaks + records; not a silent default); child has no stdin and own process group; spawn metadata finalized on every outcome; `OF_CHILD` always set | SKILL / README / adapters / CHANGELOG | `spawn_env` / `SPAWN_ENV_*` / `OF_CHILD_ENV` / `OperatorAction` | `scripts/of_adapters.py` / `scripts/of/cli/wave.py` / `tests/test_spawn_trust.py` | `spawn_env` / `OperatorAction` | — | critical | OK | keep; 0.6.7; LEARN-001 sets OF_CHILD; operator-action 0.8.4 |
| C-068 | CLI error boundary: one-line `of: error: <kind>: <message>` exit 1; `--json` emits `error` event and no prose; traceback only under `OF_DEBUG=1`; Ctrl-C exits 130 | SKILL / README / events.md / CHANGELOG | CLI `main` wrapper | `scripts/of/cli/__init__.py` / `tests/test_cli_error_boundary.py` | `main` | — | critical | OK | keep; 0.6.7 |
| C-069 | Python floor 3.11 on every public surface; CI matrix 3.11 + 3.13 | README / CONTRIBUTING / CHANGELOG | `scripts/of.py` refuse; `tests/test_python_floor.py` | `scripts/of.py` / `.github/workflows/test.yml` | | — | critical | OK | keep; 0.6.7 |
| C-070 | Spawn always sets `OF_CHILD`; `--protocol`/`--promote` refuse it (`of: error: child-forge:`); `source=leader` never written for a child; protocol lines render as untrusted quotes | SKILL / README / SLAVE / adapters | `spawned_child_id` walks live `OF_CHILD`, spawn pid/starttime registry (survives exec), then ancestor exec-env. Missing proof stamps `unauthenticated`, never `leader`. Untrusted quoting remains. Not OS-user auth. | `scripts/of/field.py` / `scripts/of/cli/wave.py` / `scripts/of/pack.py` / `tests/test_learn_provenance.py` | `spawned_child_id` / `refuse_child_forge` / `learning_provenance` | — | critical | OK | LEARN-001 [PR #41](https://github.com/pedroknigge/orderfield/pull/41); LEARN-002 0.7.1 |
| C-071 | Multi-file field mutations stage one generation, write MANIFEST (paths+hashes), then publish; crash leaves previous generation readable; recovery is idempotent | architecture / README / SKILL / troubleshooting / CHANGELOG | WAL-001 publish holds (crash before CURRENT stays previous). WAL-002 readers use CURRENT generation files; live disk is cache/tamper. Writers rematerialize CURRENT onto stale live before inherit. Immediate checkpoint and status-then-checkpoint after `OF_WAL_CRASH=after-current` keep `children_spawned=2` and packets e1/e2. Silent SPEC rewrite is still refused. Tests hash `wal/<gid>/` against MANIFEST. | `scripts/of/wal.py` / `scripts/of/field.py` / `tests/test_field_wal.py` | `_field_view_bytes` / `ensure_committed_field_view` / `_refuse_live_spec_tamper` / `_WalGeneration` | — | critical | OK | WAL-001 [PR #45](https://github.com/pedroknigge/orderfield/pull/45); WAL-002 writer 0.7.2; form in wal.py |
| C-072 | `of pack` does not default tokens=80000; `--tokens N` for N>0 dies pointing at reserved accounting; only `budget.seconds` is enforced | README / SKILL / packet.schema.json | `cmd_pack` tokens check; schema minimum 0 | `scripts/of/cli/wave.py` / `schemas/packet.schema.json` / `tests/test_budget_tokens.py` | `BudgetTokensReserved` | — | critical | OK | BUDGET-001 |
| C-073 | `of collect` / `of integrate` print owned-but-unverified binding IDs; never auto-stamp `verified_contract` | SKILL / CHANGELOG / kernel feature | collect/integrate note; `cmd_spec --verified-contract` is the stamp | `scripts/of/cli/field_cmd.py` / `tests/test_theater_fieldops.py` | `Loop001CollectIntegrate` | — | critical | OK | LOOP-001 |
| C-074 | `constraints+` and `--constraints-add` skip whitespace-normalized duplicates | SKILL / CHANGELOG | `constraint_norm` | `scripts/of/regime.py` / `scripts/of/cli/field_cmd.py` / `tests/test_theater_fieldops.py` | | — | critical | OK | DEDUPE-001 |
| C-075 | `PHASE.md` lists `done_when_mission` and `done_when_phase` separately; both empty prints `no phase criteria; of patch --done-when` | SKILL / CHANGELOG | `write_phase_md` | `scripts/of/field.py` / `tests/test_theater_fieldops.py` | `write_phase_md` | — | critical | OK | PHASE-001 |
| C-076 | Render/handoff compact the prompt ORDER view; disk packet stays full | SKILL / CHANGELOG | `render_prompt` order view | `scripts/of/pack.py` / `tests/test_theater_renderdoc.py` | `render_prompt` | — | critical | OK | RENDER-001 |
| C-077 | `of patch --backlog-undone N` sets that row `done=false`; no ghost rows | SKILL / CHANGELOG | `--backlog-undone` | `scripts/of/cli/__init__.py` / `scripts/of/cli/field_cmd.py` / `tests/test_theater_fieldops.py` | | — | critical | OK | BACKLOG-001 |
| C-078 | `of spec --add ID` leaves the ID in SPEC.md (append dated line if missing; refresh spec_hash) | SKILL / kernel feature / CHANGELOG | `cmd_spec --add` | `scripts/of/spec.py` / `scripts/of/cli/spec_cmd.py` / `tests/test_theater_renderdoc.py` | | — | critical | OK | SPEC-001 |
| C-079 | SLAVE: product comments short/factual not field diary; SKILL: do not pack a whole phase; oversized slice stays advisory | SLAVE / SKILL / CHANGELOG | doctrine text; pack does not refuse ≥800 | `SLAVE.md` / `SKILL.md` / `scripts/of/cli/wave.py` | | — | critical | OK | DOCTRINE-001 |
| C-080 | `main` requires `required_approving_review_count >= 1` and the five CI checks | CONTRIBUTING / CHANGELOG | Protection config is restored: count=1, dismiss stale, enforce admins, five checks, no force-push/delete. Adoption unproven: PR #40 and 0.7.0 #41/#42/#43/#45 merged with empty reviews after a temporary review-requirement window (human-authorized). | `CONTRIBUTING.md` | | — | critical | Partial | REVIEW-001 config OK; adoption unproven |
| C-081 | `scripts/of.py` is a one-function shim (`from of.cli import main`); internals are `scripts/of/` | architecture / DEPENDENCIES / CONTRIBUTING | file contents | `scripts/of.py` | `main` | — | critical | OK | was duplicate C-065; patched DEPENDENCIES + contributing debt |
| C-082 | #48 leftover canonical residual: collect/integrate/unpack/complete-stale resolve via `packet_residual_file` (physical field-home, else leftover `.orderfield/waves/…`) | CHANGELOG / troubleshooting | `packet_residual_file` is the sole resolver. Unpack refuses leftover residual without refund. `tests/test_sibling_residual.py`. | `scripts/of/pack.py` / `scripts/of/cli/wave.py` / `tests/test_sibling_residual.py` | `packet_residual_file` / `cmd_unpack` / `complete_stale_wave_recoverable` | — | critical | OK | #48 SIBLING-001 0.7.2 |
| C-083 | #49 `done_when_closed` is part of the integration digest; `of patch --done-when-closed` then `integrate --recompute` selects `phase` instead of replaying hold. #164: a successful `of phase` refreshes that covering digest (`PhaseDigest`) so `of next-wave` does not require `--recompute` of the prior wave | CHANGELOG / troubleshooting / SKILL | `integration_input_digest` includes `done_when_closed`; `PhaseDigest.refreshed_report`; `test_recompute_after_done_when_closed_selects_phase`; `test_phase_then_next_wave_without_recompute` | `scripts/of/regime.py` / `scripts/of/cli/field_cmd.py` / `tests/test_kernel_regime.py` | `integration_input_digest` / `PhaseDigest` | — | critical | OK | #49 #164 |
| C-084 | Saturation control: gc walks every field home; 7-day safe TTL; closed-field ephemeral immediate; tree budget + HITL `--audit`/`--keep-field`/`--drop-field`; `gc` in `MUTATING_COMMANDS`; no auto-drop of open ORDERs; not a daemon; `of doctor` / close / resume next WARN when audit is OVER or scratch is fat (`AuditPressure`; advisory, not FAIL, not a close gate) | troubleshooting / README / SKILL / CHANGELOG | `plan_field_retention` iterates `list_field_homes`; `SAFE_RETENTION_DAYS`; `drop_field_home`; `print_audit_block`; `AuditPressure`; `gc` in `MUTATING_COMMANDS`. Tests in `EpisodicRetention` / `DoctorAuditPressure`. | `scripts/of/retain.py` / `scripts/of/field.py` / `scripts/of/cli/ops.py` / `scripts/of/cli/spec_cmd.py` / `tests/test_kernel_field.py` / `tests/test_kernel_cli.py` | `plan_field_retention` / `cmd_gc` / `drop_field_home` / `AuditPressure` | — | critical | OK | SAT-001..006 0.7.3; form in retain.py; doctor/close warn 0.7.100 |
| C-085 | #54 pack continuation: a child that already owns a binding ID may pack again without `--owns-requirement` while other IDs stay unowned; a new child that owns nothing still dies; exclusive owner across different children still dies | SKILL / CHANGELOG / kernel feature | `already_owns` skip of unowned-pack gate; `mark_requirements_owned` same-child reclaim | `scripts/of/cli/wave.py` / `scripts/of/spec.py` / `tests/test_kernel_pack.py` | `cmd_pack` / `mark_requirements_owned` | — | critical | OK | #54 0.7.4 |
| C-086 | #57 successful `of integrate` stdout is one JSON object with nonempty `regime`; human notes on stderr | SKILL / CHANGELOG / kernel feature / events | `cmd_integrate` prints JSON on stdout; mission note and owned-unverified on stderr; `--json` `mission_not_applied` | `scripts/of/cli/field_cmd.py` / `tests/test_theater_fieldops.py` | `cmd_integrate` | — | critical | OK | #57 0.7.4 |
| C-087 | #55 invalid requirement id keeps `PREFIX-001`; hyphenated prefixes die and the refusal names that PREFIX must not contain `-` | SKILL / CHANGELOG / kernel feature | `require_req_id` hint when `text.count("-") > 1`; `REQ_ID_RE` unchanged | `scripts/of/spec.py` / `tests/test_spec_io.py` | `require_req_id` | — | critical | OK | #55 0.7.4 |
| C-088 | #56 skipped-learnings warning once per unchanged skipped-set fingerprint; items still never enter a prompt | SKILL / CHANGELOG / events | `_filter_learnings` skip-warn cache sidecar of `OF_LEARNINGS` | `scripts/of/learn.py` / `scripts/of/field.py` / `tests/test_learn_provenance.py` | `_filter_learnings` | — | critical | OK | #56 0.7.4 |
| C-089 | `of eval --strict --kernel` proves silent mission/phase/constraints/done-when rewrite dies and a public-surface slogan/internal/child stamp cannot close | CHANGELOG / evals / external-brief | recovery fixtures `mission-rewrite-refused`, `contrast-close-contract`, `slogan-evidence-refused`; `MissionRewriteRefused` file-level ORDER assert; `stderr_contains` on eval steps | `evals/recovery/` / `scripts/of/cli/spec_cmd.py` / `tests/test_kernel_cli.py` | `EvalInvariantSetup` / `run_recovery_eval_spec` / `MissionRewriteRefused` | — | critical | OK | 0.7.5 |
| C-090 | `of eval --strict --kernel` proves pack exclusivity: two children cannot own the same binding ID; a new child that owns nothing is refused while IDs stay unowned; same-wave `--owns-path` overlap dies; a disjoint second owner still packs | CHANGELOG / evals / external-brief | recovery fixture `pack-exclusivity-refused`; `EvalInvariantSetup.setup_pack_exclusivity`; `mark_requirements_owned`; `cmd_pack` `already_owns` gate; `same_wave_owns_path_conflict` | `evals/recovery/pack-exclusivity-refused.eval.json` / `scripts/of/cli/spec_cmd.py` / `scripts/of/spec.py` / `scripts/of/cli/wave.py` / `scripts/of/pack.py` | `EvalInvariantSetup.setup_pack_exclusivity` / `mark_requirements_owned` / `cmd_pack` / `same_wave_owns_path_conflict` | — | critical | OK | 0.7.6 |
| C-091 | `of close` refuses unless contrast is RESOLVED; success sets `spec_closed` + `done_when_closed` and writes `CLOSE.json` in the same WAL generation | CHANGELOG / evals / external-brief | `CloseProof.stamp`; `cmd_close`; `_WAL_SNAPSHOT_NAMES` includes `CLOSE.json`; recovery `atomic-close-flag-lag` | `scripts/of/cli/spec_cmd.py` / `scripts/of/wal.py` / `evals/recovery/atomic-close-flag-lag.eval.json` | `CloseProof` / `cmd_close` | — | critical | OK | 0.7.7 |
| C-092 | `.orderfield/ACTIVE` plus bind order: explicit / origin / ACTIVE / nested-over-stub; `of status` / `of resume` reflect the nested field | CHANGELOG / evals / glossary | `ActiveField`; `bind_active_field`; `of new`/`init` write ACTIVE; recovery `active-field-pointer` | `scripts/of/field.py` / `scripts/of/cli/init_cmd.py` / `scripts/of/cli/ops.py` / `evals/recovery/active-field-pointer.eval.json` | `ActiveField` / `bind_active_field` | — | critical | OK | 0.7.7 |
| C-093 | Generic done_when placeholders die at `of init` / `of patch` / `integrate --apply` `done_when+`; contrast-bound default and criteria are accepted; production-mode / day-90 close additionally requires a repo-relative runbook path in `done_when` (`RunbookPath` on `DoneWhenLint.refuse_close`; toy fields stay) | CHANGELOG / evals / external-brief / SKILL | `DoneWhenLint`; `RunbookPath`; default `of contrast RESOLVED then of close`; recovery `done-when-lint` | `scripts/of/regime.py` / `scripts/of/cli/init_cmd.py` / `scripts/of/cli/field_cmd.py` / `evals/recovery/done-when-lint.eval.json` | `DoneWhenLint` / `RunbookPathGate` | — | critical | OK | 0.7.7; Prod§15 0.7.89 |
| C-094 | Open field, empty current wave, age ≥ 7 days: `of status` / `of resume` print `signal abandoned`; field is not deleted or closed | CHANGELOG / evals / external-brief | `FieldSignal`; recovery `stale-field-abandoned` | `scripts/of/field.py` / `scripts/of/cli/ops.py` / `evals/recovery/stale-field-abandoned.eval.json` / `tests/test_kernel_field.py` | `FieldSignal` | — | critical | OK | 0.7.9 |
| C-095 | explore→build without `--force` dies; a forced skip-explore override is visible on status | CHANGELOG / evals / external-brief | `phase_transition_errors`; status prints last `phase_overrides` row; recovery `skip-explore-theater` | `scripts/of/regime.py` / `scripts/of/cli/ops.py` / `evals/recovery/skip-explore-theater.eval.json` | `cmd_status` / `phase_transition_errors` | — | critical | OK | 0.7.9 |
| C-096 | Claude / Grok / Codex / Cursor dry-run share one packet residual path; Codex argv names `residual.codex.schema.json`; collect accepts that residual | CHANGELOG / evals / external-brief | `build_spawn_argv`; recovery `multi-harness-residual`; `MultiHarnessResidual`; deep dests via C-128; basename via `ArgvRedact` (C-098) | `scripts/of_adapters.py` / `evals/recovery/multi-harness-residual.eval.json` / `tests/test_kernel_cli.py` | `build_spawn_argv` / `MultiHarnessResidual` | — | critical | OK | 0.7.9; cursor+dests 0.7.44 |
| C-097 | `of doctor` reports skill VERSION skew on existing HOME dests versus this checkout; missing dests are silent; does not invent installs; skill SKEW is advisory (WARN / exit 0) as of 0.7.39 | CHANGELOG / troubleshooting / kernel feature | `SkillVersionSkew`; doctor skill `SKEW` is WARN; `DoctorSkillVersionSkew` | `scripts/of/field.py` / `scripts/of/cli/ops.py` / `tests/test_kernel_cli.py` | `SkillVersionSkew` / `cmd_doctor` | — | critical | OK | 0.7.10; advisory 0.7.39 |
| C-098 | Spawn argv preview keeps the last path segment of `--output-schema` / `-o` and of filesystem tokens ending in `.json` (and kin) so a skill-root path longer than 80 chars still names `residual.codex.schema.json`; long prompts stay `<prompt>`; secret flags stay redacted | CHANGELOG / evals / architecture / kernel feature | `ArgvRedact`; `redact_argv`; `MultiHarnessResidual.test_matrix_stays_green_on_deep_install_dests`; recovery `multi-harness-residual` | `scripts/of/field.py` / `tests/test_kernel_cli.py` / `evals/recovery/multi-harness-residual.eval.json` | `ArgvRedact` / `redact_argv` | — | critical | OK | 0.7.11; dests 0.7.44 |
| C-099 | Interrupted multi-day field: `of resume` reconstructs the live wave from `state.wave` + packets/residuals (stale `session.json` does not win; age + in-flight is not `abandoned`); a unique open field auto-continues when `OF_SESSION_ID` differs from `ORDER.origin.session_id`; foreign gate stays when several open fields mismatch; `of init` without `--force` dies | CHANGELOG / evals / SKILL / nested-fields / principles | `resume_auto_continue_lines(open_field_count)`; recovery `multi-day-resume`; `DurableMultiDayResume`; `test_foreign_origin_gate_single_field` / `_stays_when_siblings_open` | `scripts/of/cli/ops.py` / `evals/recovery/multi-day-resume.eval.json` / `tests/test_kernel_field.py` / `tests/test_kernel_fields.py` | `cmd_resume` / `resume_auto_continue_lines` | — | critical | OK | 0.7.12 |
| C-100 | Sibling roster: `of fields` / PICK roster mark ACTIVE with `*`, print open/closed/phase/wave/packed-age, and a `choose` line (`of new` = unrelated epic; same product = `of patch` / `of spec --amend`); `--open` / `--all` / `--cursor` page many homes; no new ORDER kind | CHANGELOG / evals / nested-fields / glossary / kernel feature | `FieldRoster`; recovery `field-roster-ux`; `SiblingFields` roster tests | `scripts/of/field.py` / `scripts/of/cli/ops.py` / `scripts/of/cli/init_cmd.py` / `evals/recovery/field-roster-ux.eval.json` / `tests/test_kernel_fields.py` | `FieldRoster` / `cmd_fields` / `cmd_new` | — | critical | OK | 0.7.13 |
| C-101 | Long packs honor `budget.seconds` as the spawn wall-clock: written value is not capped; `of spawn --timeout` must match or be omitted (no silent 900 ceiling); timeout and mismatch name `of unpack` then `of pack --seconds N`; tokens stay reserved | CHANGELOG / SKILL / packet.schema / architecture / evals | `BudgetSeconds`; `cmd_pack` / `cmd_spawn`; recovery `budget-seconds-honesty`; `BudgetSecondsHonesty` | `scripts/of/cli/wave.py` / `evals/recovery/budget-seconds-honesty.eval.json` / `tests/test_budget_tokens.py` | `BudgetSeconds` / `cmd_pack` / `cmd_spawn` | — | critical | OK | 0.7.15 |
| C-102 | Mid-flight `of spec --amend` + `of patch` across waves: next packet carries dated SPEC (`spec_hash` / `spec_ref` + `## Amendment N —` UTC) and the patched constraint; the previous-wave packet is not rewritten | CHANGELOG / SKILL / evals / external-brief | `append_amendment`; `cmd_spec` / `cmd_patch` / `cmd_pack`; recovery `midflight-amend`; `MidFlightAmend`; `EvalFileAssert` | `scripts/of/spec.py` / `scripts/of/cli/spec_cmd.py` / `evals/recovery/midflight-amend.eval.json` / `tests/test_kernel_spec.py` | `append_amendment` / `cmd_spec` / `cmd_pack` / `EvalFileAssert` | — | critical | OK | 0.7.16 |
| C-103 | Collect/integrate refuse chat-dump residuals; a structured residual writes a wave report that is a reduction (`status` / `wants` / `uncertainty`), not transcript evidence | CHANGELOG / SKILL / evals / external-brief / kernel feature | `ResidualQuality`; `validate_residual`; recovery `wave-report-quality-gate`; `WaveReportQualityGate` | `scripts/of/pack.py` / `evals/recovery/wave-report-quality-gate.eval.json` / `tests/test_kernel_pack.py` | `ResidualQuality` / `validate_residual` / `WaveReportQualityGate` | — | critical | OK | 0.7.17 |
| C-104 | Whole-phase pack slogans die at `of pack` with a split/constraints fix path; length ≥800 stays advisory (packet still written) and names `of unpack` | CHANGELOG / SKILL / evals / events / kernel feature | `SliceLint`; `cmd_pack`; recovery `packet-sizing-lint`; `SliceLintGate` | `scripts/of/pack.py` / `scripts/of/cli/wave.py` / `evals/recovery/packet-sizing-lint.eval.json` / `tests/test_kernel_pack.py` | `SliceLint` / `cmd_pack` | — | critical | OK | 0.7.18 |
| C-105 | Field threshold residual on `mission` / `phase` / `constraints` / `done_when` / `workspace` forbids pack/spawn until leader `of patch` + guarded `of next-wave`; next-wave packet carries the patched field; previous-wave packets are not rewritten | CHANGELOG / SKILL / evals / external-brief / glossary | `_select_regime`; `spawn_is_blocked`; `wave_transition_errors`; recovery `threshold-stop-spawn`; `ThresholdStopSpawn` | `scripts/of/regime.py` / `scripts/of/pack.py` / `evals/recovery/threshold-stop-spawn.eval.json` / `tests/test_kernel_regime.py` | `_select_regime` / `spawn_is_blocked` / `wave_transition_errors` / `ThresholdStopSpawnEval` | — | critical | OK | 0.7.19 |
| C-106 | Spawn-host death leftovers (started-only spawn metadata, dead pid leftover, incomplete WAL generation): `of resume` reconstructs the live wave from `state.wave` + packets/residuals via CURRENT; leftovers do not invent `PACK` / `no ORDER` / `abandoned`; `of init` without `--force` dies | CHANGELOG / SKILL / evals / external-brief / glossary | `cmd_resume`; WAL CURRENT view; recovery `process-death-resume`; `ResumeAfterProcessDeath`; `ProcessDeathResume` | `scripts/of/cli/ops.py` / `scripts/of/cli/spec_cmd.py` / `evals/recovery/process-death-resume.eval.json` / `tests/test_kernel_field.py` | `cmd_resume` / `ProcessDeathResume` / `ResumeAfterProcessDeath` | — | critical | OK | 0.7.20 |
| C-107 | Weak/generic/empty done_when theater dies at init/patch/`done_when+` (punctuation-stripped platitudes) and cannot stamp `done_when_closed` when the active set (`done_when_for`) is empty or generic; contrast-bound default still closes | CHANGELOG / SKILL / evals / external-brief / close-honesty | `DoneWhenLint.body` / `refuse_close`; recovery `done-when-lint`; `DoneWhenLintRefuse` | `scripts/of/regime.py` / `scripts/of/cli/field_cmd.py` / `scripts/of/cli/spec_cmd.py` / `evals/recovery/done-when-lint.eval.json` / `tests/test_kernel_regime.py` | `DoneWhenLint` / `refuse_close` | — | critical | OK | 0.7.21 |
| C-108 | In-flight child's `packed_at` older than 7 days: `of status` / `of resume` print `packed_age`; field is not closed and the child is not unpacked; pulse STALE stays activity evidence; not a daemon | CHANGELOG / SKILL / evals / external-brief / glossary / kernel feature | `PackedAge`; recovery `packed-age-watchdog`; `PackedAgeWatchdog` | `scripts/of/field.py` / `scripts/of/cli/ops.py` / `evals/recovery/packed-age-watchdog.eval.json` / `tests/test_kernel_field.py` | `PackedAge` / `cmd_status` / `cmd_resume` | — | critical | OK | 0.7.22 |
| C-109 | `of contrast` emits a human one-pager and machine JSON from one `ContrastReport` document; a blocking row omitted from the human report or a JSON drift from those facts fails the eval/unittest; no new verb; no on-disk `CONTRAST.json` | CHANGELOG / SKILL / evals / events / close-honesty / glossary / kernel feature | `ContrastReport`; recovery `contrast-close-contract` / `contrast-close-internal`; `ContrastReportRenderer` | `scripts/of/cli/spec_cmd.py` / `evals/recovery/contrast-close-contract.eval.json` / `evals/recovery/contrast-close-internal.eval.json` / `tests/test_kernel_spec.py` | `ContrastReport` / `cmd_contrast` | — | critical | OK | 0.7.23 |
| C-110 | Multi-wave field: `of wave list` marks the live `state.wave` with `*`; `of wave show [N]` names packets/residuals and whether that wave is live; no new schema or ORDER field; status/resume stay one-screen on the live wave | CHANGELOG / SKILL / evals / events / glossary / kernel feature | `WaveRoster`; recovery `wave-list-show`; `WaveRosterListShow` | `scripts/of/field.py` / `scripts/of/cli/ops.py` / `evals/recovery/wave-list-show.eval.json` / `tests/test_kernel_field.py` | `WaveRoster` / `cmd_wave` | — | critical | OK | 0.7.24 |
| C-111 | `of doctor` in one pass names skill VERSION skew, ACTIVE pointer/stub skew, leftover root ORDER.json migrate-required, stale packs (`packed_age` / `order_rev`), and tree-audit OVER / fat scratch; pack lines include field id + wave; two or more sibling homes open without CLOSE.json are hygiene WARN (not FAIL); closed-field historical packs and audit OVER are informational (not FAIL); missing skill dests stay silent; doctor does not write ACTIVE; no new schema or CLI flag; ACTIVE/stub/leftover-root/open-field pack SKEW still FAIL; skill SKEW is advisory (C-123); cited plan docs (`docs/plans/…`) that are mtime-stale vs last integrate print `docs_sync stale|pending|findings` (`PlanDocSync`; not FAIL; not a close gate) | CHANGELOG / troubleshooting / kernel feature / evals | `DoctorSkew`; `AuditPressure`; `PlanDocSync`; field `SKEW` / `migrate required` / open-field `packed_age` fails; open-sibling `no CLOSE` WARN; closed historical note; `DoctorOnePassSkew`; `DoctorAuditPressure`; `DoctorPlanDocSync`; recovery `doctor-one-pass-skew` / `doctor-closed-historical` / `plan-doc-sync` | `scripts/of/field.py` / `scripts/of/cli/ops.py` / `scripts/of/retain.py` / `scripts/of/regime.py` / `evals/recovery/doctor-one-pass-skew.eval.json` / `evals/recovery/doctor-closed-historical.eval.json` / `evals/recovery/plan-doc-sync.eval.json` / `tests/test_kernel_cli.py` | `DoctorSkew` / `AuditPressure` / `PlanDocSync` / `cmd_doctor` | — | critical | OK | 0.7.25; skill advisory 0.7.39; closed historical 0.7.74; audit pressure 0.7.100; open hygiene 0.7.101; plan-doc sync 0.8.1 |
| C-112 | Orphan packed child (closed field / leftover home / inapplicable order / stale prior wave, residual missing) is named by `of retain` and unlinked only by explicit `of gc` with `gc-stamp.json` `orphans[]` plus the `orphan packed` needle; resume auto-gc never unlinks packets; open current-wave in-flight is not an orphan | CHANGELOG / SKILL / evals / troubleshooting / kernel feature | `OrphanPacked`; recovery `orphan-packed-cleanup`; `OrphanPackedCleanup` | `scripts/of/retain.py` / `scripts/of/cli/ops.py` / `evals/recovery/orphan-packed-cleanup.eval.json` / `tests/test_kernel_field.py` | `OrphanPacked` / `cmd_gc` / `cmd_retain` / `maybe_safe_gc` | — | critical | OK | 0.7.26 |
| C-113 | Leftover `.orderfield/ORDER.json` next to `fields/<id>/` is not a live field: `list_field_homes` omits it; `--field` of a different-id stub dies; `of new` does not promote it; `of migrate` archives to `ORDER.json.stub` (never silent delete); status/resume/fields print `root_stub`; `find_root` refuses a closer leftover inside a parent field tree | CHANGELOG / SKILL / evals / nested-fields / glossary / kernel feature | `RootStub`; recovery `root-stub-ambiguous`; `RootStubAmbiguous` | `scripts/of/field.py` / `scripts/of/cli/ops.py` / `evals/recovery/root-stub-ambiguous.eval.json` / `tests/test_kernel_fields.py` | `RootStub` / `bind_active_field` / `cmd_migrate` | — | critical | OK | 0.7.27 |
| C-114 | `of status --json` emits one parseable live-wave JSON object from the same `StatusReport` document the human screen reads (`FieldSignal` / `PackedAge` / `RootStub` / requirement counts); no `STATUS.json`; no token/runtime theater fields; `--json` / `OF_JSON=1` `status` event shares those facts; human `of status` stays one-screen | CHANGELOG / SKILL / evals / events / kernel feature | `StatusReport`; recovery `status-json`; `StatusReportJson` | `scripts/of/cli/ops.py` / `evals/recovery/status-json.eval.json` / `tests/test_kernel_field.py` | `StatusReport` / `cmd_status` | — | critical | OK | 0.7.28 |
| C-115 | Three-wave residual loop: pack → structured residual → collect → integrate → next-wave; mid-flight `of spec --amend` + `of patch` after wave 1 lands on wave-2/3 packets (`spec_hash` / `spec_ref` + dated `## Amendment 1 —`); wave-1 packet is not rewritten; wave 3 stays in-flight (`collect` MISSING, `next-wave` dies) | CHANGELOG / SKILL / evals / external-brief / kernel feature | `MultiWaveResidualEval`; recovery `multi-wave-residual`; `MultiWaveResidualLoop` | `scripts/of/cli/spec_cmd.py` / `evals/recovery/multi-wave-residual.eval.json` / `tests/test_kernel_spec.py` | `MultiWaveResidualEval` / `close_child` / `EvalInvariantSetup.write_bound_residual` | — | critical | OK | 0.7.29 |
| C-116 | `of new --parent` stamps optional `ORDER.parent` on a new sibling home (phase of an open epic); plain `of new` stays an unrelated epic; `of close` returns `.orderfield/ACTIVE` to the live parent; status/resume/fields print `parent`; no `of merge`; homes stay flat under `fields/<id>/` | CHANGELOG / SKILL / evals / nested-fields / glossary / kernel feature | `NestedField`; recovery `nested-field-lifecycle`; `NestedFieldLifecycle` | `scripts/of/field.py` / `scripts/of/cli/init_cmd.py` / `scripts/of/cli/spec_cmd.py` / `evals/recovery/nested-field-lifecycle.eval.json` / `tests/test_kernel_fields.py` | `NestedField` / `cmd_new` / `cmd_close` | — | critical | OK | 0.7.30 |
| C-117 | `of handoff` without `--packet` emits a mid-epic field packet from one `HandoffReport` document (next legal action, in-flight packet paths, pulse, checkpoint summary) for a human or next harness; `--json` is the machine object; child prompt stays `of handoff --packet`; does not unpack; no `HANDOFF.json` | CHANGELOG / SKILL / evals / events / glossary / kernel feature | `HandoffReport`; recovery `mid-epic-handoff`; `MidEpicHandoffPacket` | `scripts/of/cli/ops.py` / `scripts/of/cli/wave.py` / `evals/recovery/mid-epic-handoff.eval.json` / `tests/test_kernel_field.py` | `HandoffReport` / `cmd_handoff` | — | critical | OK | 0.7.31 |
| C-118 | Closed field archives without losing contrast trail: `of gc --archive-field <id>` moves `.orderfield/fields/<id>/` to `.orderfield/archive/<id>/` keeping `CLOSE.json` / SPEC / REQUIREMENTS / ORDER; `--drop-field` dies while `CLOSE.json` exists unless `--force --reason`; retain/gc keep archived trail (`closed-field-archive`); apply will not unlink `.orderfield/archive/`; `--field` of an archived id dies; no new verb; not `of merge` | CHANGELOG / SKILL / evals / troubleshooting / kernel feature | `ClosedFieldArchive`; recovery `closed-field-archive`; `ClosedFieldArchiveTrail` | `scripts/of/retain.py` / `scripts/of/cli/ops.py` / `evals/recovery/closed-field-archive.eval.json` / `tests/test_kernel_field.py` | `ClosedFieldArchive` / `cmd_gc` / `drop_field_home` | — | critical | OK | 0.7.32 |
| C-119 | While residual is MISSING — or a started-only spawn dominates a leftover residual — `of status` / `of resume` / `of pulse` print `running` (residual MISSING; harness chrome is not the field); a prior collect-refuse file does not hide a live re-spawn; human status names per-child pulse + `next`; `of status --json` carries `in_flight_detail[]` + `next` from the same `StatusReport` document; no daemon / supervisor / new schema | CHANGELOG / SKILL / evals / events / glossary / kernel feature / external-brief | `InFlightSignal`; `SpawnRecord.flying`; recovery `in-flight-visibility`; `InFlightVisibility` | `scripts/of/cli/ops.py` / `scripts/of/field.py` / `evals/recovery/in-flight-visibility.eval.json` / `tests/test_kernel_field.py` | `InFlightSignal` / `SpawnRecord` / `StatusReport` / `cmd_status` / `cmd_resume` / `pulse_once` | — | critical | OK | 0.7.33; leftover residual 0.7.91 |
| C-120 | `of pack --explain` dry-runs `SliceLint` and prints why a slice is oversized (length ≥800 advisory; whole-phase slogan refused) without writing a packet or charging `children_spawned`; human stdout first; `of --json` extends the existing `pack` event with `explain=true` / `written=false`; no new schema / verb / token budget | CHANGELOG / SKILL / evals / events / kernel feature | `SliceLint.document` / `human`; `cmd_pack`; recovery `packet-sizing-explain`; `SliceLintExplain` | `scripts/of/pack.py` / `scripts/of/cli/wave.py` / `evals/recovery/packet-sizing-explain.eval.json` / `tests/test_kernel_pack.py` | `SliceLint` / `cmd_pack` | — | critical | OK | 0.7.34 |
| C-121 | Adversarial corpus locks dual-truth close, fake token theater, and unpack-of-reporter on existing gates: child-forged `spec_closed` / `--tokens N>0` / unpack after residual cannot write `CLOSE.json` or delete the packet; spawn cost disclaimer is not a budget; `file_missing` fails if a proof file appears; no new CLI / schema / token ceiling | CHANGELOG / SKILL / evals / close-honesty / external-brief / kernel feature | `EvalFileAssert.absent`; recovery `adversarial-dual-truth`; `AdversarialDualTruthCorpus` | `scripts/of/cli/spec_cmd.py` / `evals/recovery/adversarial-dual-truth.eval.json` / `tests/test_kernel_spec.py` | `EvalFileAssert` / `cmd_close` / `cmd_unpack` / `refuse_nonzero_tokens` | — | critical | OK | 0.7.37 |
| C-122 | `of pack --out` accepts the logical `.orderfield/waves/…` contract path or the physical `.orderfield/fields/<id>/waves/…` path the kernel prints; comparison runs after `physical_field_rel`; a refuse names both expected forms; no new verb / schema / token budget | CHANGELOG / SKILL / evals / kernel feature | `PackOutPhysicalNested`; `physical_field_rel` | `scripts/of/cli/wave.py` / `scripts/of/field.py` / `tests/test_sibling_field_roundtrip.py` | `cmd_pack` / `physical_field_rel` / `canonical_packet_rel` | — | critical | OK | 0.7.38 |
| C-123 | `of doctor` skill VERSION SKEW alone is advisory (`WARN` / exit 0, `ok_field` / `ok_skills` on the doctor event); field/schema/lock/symlink/kernel still FAIL; `of pack` / child `of handoff` print `residual (awaiting)=`; field handoff prints `residual    MISSING` plus JSON `residual_path`; `of fields` labels first-home `first` not `legacy` | CHANGELOG / SKILL / troubleshooting / evals / events / nested-fields / kernel feature | `DoctorSkillVersionSkew`; `DoctorOnePassSkew`; `MidEpicHandoffPacket`; recovery `doctor-advisory-ux` | `scripts/of/cli/ops.py` / `scripts/of/cli/wave.py` / `scripts/of/field.py` / `evals/recovery/doctor-advisory-ux.eval.json` / `tests/test_kernel_cli.py` | `cmd_doctor` / `HandoffReport` / `FieldRoster._home_rel` | — | critical | OK | 0.7.39 |
| C-124 | In-flight children append milestone lines to existing `scratch/<id>/PULSE`; `of status` / `of resume` / `of pulse` print the last 1–3 under `running`; missing/empty `PULSE` stays `running` without crash; `of status --json` `in_flight_detail[]` carries the same `progress` list; SKILL teaches a leader one-liner each HOLD+in-flight turn; no new file / schema / daemon | CHANGELOG / SKILL / SLAVE / evals / events / glossary / kernel feature / external-brief | `PulseProgress`; recovery `in-flight-visibility`; `InFlightVisibility` | `scripts/of/cli/ops.py` / `evals/recovery/in-flight-visibility.eval.json` / `tests/test_kernel_field.py` | `PulseProgress` / `InFlightSignal` / `StatusReport` / `cmd_status` / `cmd_resume` / `pulse_once` | — | critical | OK | 0.7.40 |
| C-125 | `of fields` lists in-flight packs (residual MISSING, or a started-only spawn over a leftover residual) across open sibling homes; `of fields --json` is one parseable dashboard object from `PackRoster`; `--json` / `OF_JSON=1` `fields` event shares those facts; closed-field leftovers and completed residuals are omitted; `of status --json` stays one bound field; no new verb / schema / daemon / Notion push | CHANGELOG / SKILL / evals / events / nested-fields / glossary / kernel feature / external-brief | `PackRoster`; recovery `cross-field-pack-roster`; `PackRosterCrossField` | `scripts/of/field.py` / `scripts/of/cli/ops.py` / `evals/recovery/cross-field-pack-roster.eval.json` / `tests/test_kernel_fields.py` | `PackRoster` / `FieldRoster` / `DoctorSkew` / `cmd_fields` | — | critical | OK | 0.7.41; leftover residual 0.7.91 |
| C-126 | `of close --checklist` prints one `CloseChecklist` document (contrast gate + residual empty) and does not stamp; `of close` refuses while contrast is OPEN or any packed child residual is MISSING; ready dry-run stays unstamped; success still writes `spec_closed` + `done_when_closed` + `CLOSE.json` together; reuses `ContrastReport` + `WaveRoster`; SKILL teaches `--checklist` before stamp; no supervisor / `CHECKLIST.json` / new verb | CHANGELOG / SKILL / evals / events / glossary / long-mission / close-honesty / kernel feature / external-brief | `CloseChecklist`; recovery `multi-wave-close-checklist`; `CloseChecklistProof` | `scripts/of/cli/spec_cmd.py` / `evals/recovery/multi-wave-close-checklist.eval.json` / `tests/test_kernel_spec.py` | `CloseChecklist` / `cmd_close` / `ContrastReport` / `WaveRoster` | — | critical | OK | 0.7.42 |
| C-127 | One-sitting mortal install reuses `install.sh` then the installed `of doctor`; `--global` lands under `$HOME`; `--root PATH` is hermetic and does not touch `$HOME`; exit 0 only when doctor prints `ok` (kernel + skills); stdout names the disk contract (`.orderfield/`, `of resume`, no supervisor / `of merge`); SKILL/alias teach the script when a human asks install or verify; no new CLI / pip / daemon | CHANGELOG / SKILL / README / PUBLISH / demo / architecture | `MortalInstallDemo`; `docs/demo/mortal-install.sh` | `docs/demo/mortal-install.sh` / `docs/demo/mortal-install.md` / `tests/test_packaging.py` | `MortalInstallDemo` | — | normal | OK | 0.7.43 |
| C-128 | Claude / Codex / Cursor `--dry-run` share one packet residual when `of.py` is the installed skill copy under `~/.claude` / `~/.agents` / `~/.cursor/skills/orderfield` (long HOME dests, not a pip path); Codex argv still names `residual.codex.schema.json`; dest relpaths are `SkillVersionSkew` install dests; no PATH mock / second eval engine | CHANGELOG / SKILL / evals / external-brief / adapters feature | `MultiHarnessResidual.dest_rel` / `stage_skill`; recovery `multi-harness-residual` cursor step; `test_matrix_stays_green_on_deep_install_dests` | `tests/test_kernel_cli.py` / `evals/recovery/multi-harness-residual.eval.json` / `scripts/of/field.py` | `MultiHarnessResidual` / `SkillVersionSkew` / `ArgvRedact` | — | critical | OK | 0.7.44 |
| C-129 | Close-is-proof RFC enumerates residual empty + contrast RESOLVED + one `CLOSE.json` stamp as the durable close contract; residual empty is not the close; SKILL/alias teach `docs/close-is-proof.md`; honesty templates and the long-mission walk stay on their pages; cites existing evals only; no new CLI / schema / fixture / supervisor | CHANGELOG / SKILL / alias / README / AGENTS / close-is-proof / close-honesty / long-mission / external-brief / glossary | `CloseIsProofRfc`; recovery `multi-wave-close-checklist` / `contrast-close-contract` / `atomic-close-flag-lag` / `adversarial-dual-truth` | `docs/close-is-proof.md` / `SKILL.md` / `of/SKILL.md` / `tests/test_gb_protocol.py` | `CloseIsProofRfc` | — | normal | OK | 0.7.45 |
| C-130 | Once per day, if a newer release exists than the installed VERSION, `of status` / `resume` / `pulse` print one stderr ask and `of doctor` prints an `update` section (TTY prompts `[y/N]`); on yes, `install.sh --global --from-release` fetches the GitHub tag archive and verifies SHA256SUMS; at most one ask per day (`asked_at`); never a silent auto-update; `OF_NO_UPDATE_CHECK=1` disables; silent offline; SKILL/alias teach ask-then-consent; no daemon / cron / supervisor / new verb | CHANGELOG / SKILL / alias / README / troubleshooting / demo / kernel feature / evals | `UpdateAsk`; `UpdateAskDaily`; `InstallPin.test_from_release_ignores_local_checkout` | `scripts/of/field.py` / `scripts/of/cli/ops.py` / `install.sh` / `tests/test_kernel_cli.py` / `tests/test_packaging.py` | `UpdateAsk` / `cmd_doctor` / `maybe_notify_update` | — | critical | OK | 0.7.46 |
| C-131 | Optional per-task model hints after explicit consent: `of patch --model-hints field\|wave\|off` writes `ORDER.adapter_hints`; `of pack --model-tier` / `--model` writes `packet.adapter_hints`; spawn passes `--model` for adapters in `MODEL_FLAG_ADAPTERS` when the packet names one; Claude maps cheap→haiku / frontier→opus; Cursor has no cheap/frontier alias (catalog: no frontier row) and refuses a consented tier without `--model` so spawn cannot go QUIET with no log; grok/agy/codex tier-only stay no-op; orca task-create / qwen / opencode / generic no-op; no consent → no hint → no silent switch; not a router / supervisor / token budget | CHANGELOG / SKILL / alias / adapters / kernel feature / events / glossary | `AdapterHints`; `AdapterHintsCli`; recovery `cursor-tier-model` | `scripts/of_adapters.py` / `scripts/of/cli/wave.py` / `scripts/of/cli/field_cmd.py` / `tests/test_adapter_hints.py` / `evals/recovery/cursor-tier-model.eval.json` | `AdapterHints` / `build_spawn_argv` / `cmd_pack` / `cmd_patch` | — | critical | OK | 0.7.47; cursor refuse 0.8.5 |
| C-132 | Efficiency signal scores landed live-wave residuals (quality `ok`/`escalate`/`rework` × optional harness-reported `residual.usage`); `of status` / `resume` / `doctor` may propose uptier/downtier and name `of patch --model-hints` / `--model-tier`; never writes `adapter_hints`; never compares usage to reserved `budget.tokens`; `--tokens N>0` still dies; missing usage is valid; no silent switch / token ceiling / session ledger | CHANGELOG / SKILL / alias / efficiency-signal / glossary / architecture / kernel feature / events / evals | `EfficiencySignal`; recovery `efficiency-signal`; `EfficiencySignalProof` / `EfficiencySignalUnit` | `scripts/of/cli/ops.py` / `schemas/residual.schema.json` / `evals/recovery/efficiency-signal.eval.json` / `tests/test_efficiency_signal.py` / `docs/efficiency-signal.md` | `EfficiencySignal` / `cmd_status` / `cmd_resume` / `cmd_doctor` | — | critical | OK | 0.7.48; AdapterBalance unknown 0.7.84 |
| C-133 | `of contrast --diff` prints one `ContrastDiff` narrative from the same `ContrastReport` rows plus `SpecDiff` flags; DELIVERED means owned not close-ok; VERIFIED_INTERNAL is not the public contract; RESOLVED is not CLOSED; ORDER omission can remain after the close gate and is named; no theater (`mission complete` / `all delivered` / `CLOSED`); default one-pager + machine JSON stay; SKILL/alias teach `--diff`; no new verb / `CONTRAST.json` / supervisor | CHANGELOG / SKILL / alias / README / evals / events / glossary / kernel feature / architecture | `ContrastDiff`; recovery `contrast-diff-narrative`; `ContrastDiffNarrative` | `scripts/of/cli/spec_cmd.py` / `scripts/of/spec.py` / `evals/recovery/contrast-diff-narrative.eval.json` / `tests/test_kernel_spec.py` | `ContrastDiff` / `SpecDiff` / `cmd_contrast` | — | critical | OK | 0.7.49 |
| C-134 | Published SKILL / `/of` alias / README cannot use marketing theater; advertised claims-matrix truth score must match the table and stay ≤98%; `python3 docs/audit/check-claims.py` (`ClaimsHonesty`) is the gate and is wired into `validate-skill.sh`; critical Contradicted fails; `ClaimsHonestyGate` on `of eval --strict --kernel`; no new CLI / schema / supervisor | CHANGELOG / SKILL / alias / README / AGENTS / audit / evals / kernel feature / architecture | `ClaimsHonesty`; `ClaimsHonestyGate` | `docs/audit/check-claims.py` / `scripts/validate-skill.sh` / `tests/test_claims_honesty.py` / `SKILL.md` / `of/SKILL.md` | `ClaimsHonesty` / `ClaimsHonestyGate` | — | critical | OK | 0.7.50 |
| C-135 | README product surface leads with an authority-axis hero (anyone can persist a plan; only the leader may change it), then typical problems → what Orderfield does (use-case table) before kernel abstractions, install checksum recipes, and compared-to; Compared-to includes planning-with-files (same disk-plan category, different authority-kernel product through `of`); Haken “slow field constrains the fast” stays below as an analogy (not a science claim; slaving-by-contract through `of`, cooperative CLI, not a jail) with a link to `references/principles.md`; SKILL / `/of` alias / appendix point at that positioning and SHA-256 pin → first close (unpinned `npx skills add` is not the trusted path); no new CLI / schema / supervisor | CHANGELOG / SKILL / alias / appendix / README / AGENTS / architecture / kernel feature / roadmap | `ReadmeProductSurface` | `README.md` / `SKILL.md` / `of/SKILL.md` / `references/skill-appendix.md` / `tests/test_packaging.py` / `references/principles.md` | `ReadmeProductSurface` | — | normal | OK | 0.7.96 |
| C-136 | README Mid-flight H2 (`## Mid-flight, the plan can change without dying`) sits after the opening product block and before `## Install`; own H2; three kinds of change plus sibling fields stay intact; not folded into the problem table; Haken analogy stays below; SKILL / `/of` alias point at that order; no new CLI / schema / supervisor | CHANGELOG / SKILL / alias / README / AGENTS / architecture / kernel feature / roadmap | `ReadmeProductSurface` | `README.md` / `SKILL.md` / `of/SKILL.md` / `tests/test_packaging.py` | `ReadmeProductSurface` | — | normal | OK | 0.7.52 |
| C-137 | Leader skill must propose a cheap vs frontier worker distribution in chat before the first multi-role pack (or when re-planning roles); human confirms; then `of patch --model-hints` / pack `--model-tier` / `--model`; `efficiency propose` stays ask-only; never a silent switch; kernel stays 0.7.47 AdapterHints + 0.7.48 EfficiencySignal (consent + argv passthrough); README opening names the consent propose; no new CLI / schema / router / supervisor | CHANGELOG / SKILL / alias / README / AGENTS / efficiency-signal / glossary / adapters feature / kernel feature / roadmap | `SkillLeaderInitiative`; `ReadmeProductSurface` | `SKILL.md` / `of/SKILL.md` / `README.md` / `tests/test_packaging.py` | `SkillLeaderInitiative` | — | normal | OK | 0.7.53 |
| C-138 | Skill YAML `description` (and `compatibility` when present) is double-quoted so agy / npx skills discover the skill; `install.sh` `agy_dests` includes Shared `~/.gemini/skills/<name>` plus Global `~/.gemini/antigravity-cli/skills/<name>`; `~/.gemini/config/skills/<name>` is optional legacy when that parent exists; alias `/of` follows the same dests; `SkillVersionSkew` / `of doctor` scan Shared; no new CLI / schema / supervisor | CHANGELOG / SKILL / alias / README / adapters / AGENTS | `SkillFrontmatterQuotedGate`; `agy_dests`; `SkillVersionSkew.AGY_REL` | `SKILL.md` / `of/SKILL.md` / `install.sh` / `scripts/of/field.py` / `tests/test_packaging.py` | `agy_dests` / `SkillVersionSkew` | — | critical | OK | 0.7.54 |
| C-139 | Residual schema accepts optional top-level `v` (integer; kernel `--json` document version) and `of collect` / `validate_residual` ignore it; residuals without `v` stay valid; other unexpected keys still fail; Codex `residual.codex.schema.json` stays the strict derivative; reports still emit `v`; no new CLI / supervisor | CHANGELOG / SLAVE / AGENTS / kernel feature | `ResidualSchemaContracts.test_optional_document_v_is_ignored`; `test_collect_accepts_residual_with_document_v` | `schemas/residual.schema.json` / `schemas/residual.codex.schema.json` / `scripts/of/pack.py` / `tests/test_kernel_regime.py` | `validate_residual` | — | critical | OK | 0.7.55 |
| C-140 | `ResidualQuality` byte cap on `residual.evidence` / `proposed_patch.notes` fires only when shape is ambiguous (no `TURN_RE` hits, and no counts/paths/shas); honest structured evidence over 4000 chars collects; transcript dumps still refuse; a size/line refuse names `scratch/<child>/notes.md` + re-spawn the same packet; no new CLI / schema / supervisor | CHANGELOG / SKILL / alias / SLAVE / glossary / kernel feature / evals | `ResidualQuality`; `WaveReportQualityGate.test_structured_evidence_over_old_char_cap_is_accepted`; `test_transcript_dump_still_refused_with_structured_markers`; recovery `wave-report-quality-gate` | `scripts/of/pack.py` / `tests/test_kernel_pack.py` / `evals/recovery/wave-report-quality-gate.eval.json` / `SKILL.md` / `of/SKILL.md` | `ResidualQuality` / `dump_errors` | — | critical | OK | 0.7.56 |
| C-141 | Consented `adapter_hints` pass `--model NAME` for grok and agy via the same `AdapterHints.spawn_model` / `spawn_flags` path as claude/codex/cursor; flags precede `-p`; named model only (no invented cheap/frontier aliases; tier-only is no-op argv); no consent → argv unchanged; `of doctor` lists grok/agy on pass; orca/qwen/opencode/generic stay no-op; not a router / supervisor / silent switch | CHANGELOG / SKILL / alias / adapters / kernel feature / glossary / AGENTS | `AdapterHints.MODEL_FLAG_ADAPTERS`; `AdapterHintsCli`; `AdapterHintsArgv` | `scripts/of_adapters.py` / `tests/test_adapter_hints.py` / `SKILL.md` / `of/SKILL.md` / `references/adapters.md` | `AdapterHints` / `build_spawn_argv` | — | critical | OK | 0.7.57 |
| C-142 | While any residual is MISSING, `of status` / `of resume` print a reused `InFlightSignal.speak_line()` (`speak  quote a PULSE line above to the user; do not claim done while running`) after the existing `running` banner + `PulseProgress` lines, replacing the old manual `activity  of pulse` pointer; SKILL / alias teach the turn-end duty (quote live PULSE before claiming done; every in-flight turn, not only `next=HOLD`); reuses 0.7.40 pulse machinery; no new CLI / schema / event / daemon | CHANGELOG / SKILL / alias / kernel feature | `InFlightSignal.speak_line`; `InFlightVisibility.test_status_and_resume_tell_leader_to_quote_pulse` | `scripts/of/cli/ops.py` / `tests/test_kernel_field.py` / `SKILL.md` / `of/SKILL.md` | `InFlightSignal` / `cmd_status` / `cmd_resume` | — | critical | OK | 0.7.58 |
| C-143 | Leader skill must ask same-harness categories vs multi-harness mix in chat before the first multi-role pack (or when re-planning); human confirms; then `of patch --harness` or stay on session, or `of detect` then spawn only from that PATH list; never a silent mix; SKILL / `/of` / appendix teach when to mix vs roles on one harness with claude/codex/cursor/grok/agy examples and bind mix language to `of pack` / `of spawn` / `of collect` / `of contrast` / `of close` / `of doctor` + detect consent (PATH≠auth); captions-only mix pages fail `SkillHarnessMix`; kernel stays `cmd_detect` + `ORDER.harness` (no `of ask` / new catalog / router / supervisor); README opening names the consent ask; C-014 stays Partial | CHANGELOG / SKILL / alias / README / AGENTS / adapters feature / kernel feature / roadmap | `SkillHarnessAsk`; `SkillHarnessMixPlaybook` | `SKILL.md` / `of/SKILL.md` / `README.md` / `references/skill-appendix.md` / `scripts/living_map.py` / `tests/test_packaging.py` | `SkillHarnessAsk` / `SkillHarnessMix` | — | normal | OK | 0.7.83 |
| C-144 | Living model catalog (`docs/model-catalog.md` + `docs/model-catalog.json`) lists harness / model id / tier hint / public `$/unit` / notes / `last_checked` with cited public price sheets or explicit unknown; SKILL / `/of` consult the catalog before proposing cheap vs frontier or asking a mix; ask consent; no invented IQ ranks or `$/MTok`; `of doctor` prints one advisory pointer; kernel does not route spawn or enforce `budget.tokens` from the sheet; no `of catalog` / router / supervisor | CHANGELOG / SKILL / alias / README / AGENTS / model-catalog / glossary / adapters / kernel feature / roadmap | `ModelCatalogHonesty`; `SkillModelCatalogConsult` | `docs/model-catalog.md` / `docs/model-catalog.json` / `scripts/of/model_catalog.py` / `SKILL.md` / `of/SKILL.md` / `tests/test_packaging.py` | `ModelCatalog` | — | normal | OK | 0.7.61 |
| C-145 | `of spawn` of documented stream-json / JSON event streams (claude `-p` + `--output-format stream-json --verbose`, cursor `--output-format stream-json`, codex `--json`, grok `--output-format streaming-json`) appends honest ≤10-word milestones to the same `scratch/<id>/PULSE` (`StreamJson` + `PulseProgress.append`); residual extract stays the existing stdout path (`extract_json_object` / stream residual / `SpawnResidual`, including timeout); timeout uses daemon reader threads and a bound join (`ChildIO`) so spawn metadata is finalized (`outcome` + `exit` + `ended_at`) on exit, timeout, and missing binary — including `--adapter grok`; agy/qwen/opencode keep a JSON blob (no invented stream-json; grok's value is the documented `streaming-json`); SKILL / `/of` / SLAVE teach the same file; detect remains PATH≠auth; no new CLI / schema / event / daemon / supervisor / token ceiling | CHANGELOG / SKILL / alias / SLAVE / adapters / glossary / kernel feature / roadmap | `StreamJsonParse`; `PulseProgressAppend`; `StreamJsonSpawn`; `StreamJsonPulseSkill`; `GrokAdapterSpawn` | `scripts/of_adapters.py` / `scripts/of/cli/wave.py` / `scripts/of/cli/ops.py` / `tests/test_stream_json.py` / `SKILL.md` / `of/SKILL.md` | `StreamJson` / `PulseProgress` / `SpawnResidual` / `ChildIO` / `run_child` | — | critical | OK | 0.7.71 |
| C-146 | Conservative `agy` spawn copies nonempty harness `denied_actions` from the existing `--output-format json` envelope into optional `residual.denied_actions` (`AgyDeniedActions` + `StreamJson.parse_line`); missing or empty is omit — not approval; `yolo` does not copy and conservative argv still emits no bypass; never invents a residual just to hold the list. For any missing residual, `CollectDiagnostic` prints pending/unavailable plus known adapter / trust / outcome and actual denials; conservative headless permissions are an adapter-tied possibility, never proof that children cannot write; successful conservative scratch/residual delivery stays supported. SKILL / `/of` / appendix teach the wording; no new CLI / supervisor / token ceiling | CHANGELOG / SKILL / alias / appendix / SLAVE / adapters / architecture / kernel feature / roadmap | `AgyDeniedActionsParse`; `AgyDeniedActionsSpawn`; `AgyDeniedActionsSkill`; `ResidualDeniedActionsSchema`; `CollectSurvivesMissingResiduals`; `SkillCollectConservativeDiagnostic` | `scripts/of_adapters.py` / `scripts/of/cli/wave.py` / `schemas/residual.schema.json` / `tests/test_agy_denied_actions.py` / `tests/test_kernel_pack.py` / `tests/test_packaging.py` / `SKILL.md` / `of/SKILL.md` / `references/skill-appendix.md` | `AgyDeniedActions` / `CollectDiagnostic` / `cmd_spawn` / `cmd_collect` | — | critical | OK | 0.7.75 |
| C-147 | `of spawn --adapter agy` passes `--json-schema` to the same `residual.codex.schema.json` Codex uses (`OutputSchema`); flags precede `-p`; `StreamJson.residual` also reads `structured_output`; Claude omit (inline `--json-schema` only; keep stream-json PULSE; do not fake a path); Qwen omit (structured_output tool, not residual delivery); Codex `--output-schema` + `-o` intact; SKILL / `/of` teach the wire and the omit; no new schema file / CLI / supervisor / token ceiling | CHANGELOG / SKILL / alias / adapters / architecture / kernel feature / roadmap | `OutputSchemaArgv`; `OutputSchemaExtract`; `OutputSchemaRedact`; `OutputSchemaSkill` | `scripts/of_adapters.py` / `scripts/of/field.py` / `tests/test_output_schema.py` / `SKILL.md` / `of/SKILL.md` / `references/adapters.md` | `OutputSchema` / `build_spawn_argv` / `StreamJson.residual` / `ArgvRedact` | — | critical | OK | 0.7.64 |
| C-148 | `OF_TRUST=plan` maps documented native flags via existing `_TRUST_FLAGS`: cursor `--mode plan`, agy `--mode plan`, grok `--sandbox read-only`; Claude / Codex / Qwen plan rows stay; Cursor / OpenCode / Grok `auto-edit`/`auto` stay conservative (no accept-edits; bypass is yolo only); Claude `auto` stays `acceptEdits` (classifier `--permission-mode auto` is account/model gated); Codex `--ask-for-approval` is not a reliable `exec` flag; Orca / generic stay untranslated; SKILL / `/of` / adapters table stay lockstep; no invented flags / new CLI / supervisor / token ceiling | CHANGELOG / SKILL / alias / adapters / kernel feature / roadmap | `TrustNativeFlags`; `TrustMatrixInProcess.test_plan_maps_to_plan_modes` | `scripts/of_adapters.py` / `tests/test_spawn_trust.py` / `SKILL.md` / `of/SKILL.md` / `references/adapters.md` | `trust_flags` / `_TRUST_FLAGS` | — | critical | OK | 0.7.65 |
| C-149 | Adapter resume/continue emits documented `--resume ID` (claude/cursor) only when optional `residual.session_id` is already nonempty (`AdapterResume`); missing residual / blank / `-1`/`0` is a fresh spawn; never invent an id; never emit `--continue`; not `ORDER.origin.session_id`; Codex/agy/grok/qwen/opencode/orca/generic omit; Codex `--output-schema` omits the key; SKILL / `/of` teach the gate; no new CLI / supervisor / token ceiling | CHANGELOG / SKILL / alias / adapters / glossary / kernel feature / roadmap | `AdapterResumeGate`; `AdapterResumeSpawn`; `AdapterResumeSkill`; `ResidualSessionIdSchema` | `scripts/of_adapters.py` / `scripts/of/cli/wave.py` / `schemas/residual.schema.json` / `tests/test_adapter_resume.py` / `SKILL.md` / `of/SKILL.md` | `AdapterResume` / `build_spawn_argv` | — | critical | OK | 0.7.66 |
| C-150 | Published skill is a short always-loaded `SKILL.md` core plus `references/skill-appendix.md`; hosts load the core; the core table names every kernel verb and tells the leader to read the appendix before pack/spawn/contrast/close; appendix **Production mode** teaches production-mode invariants and Gate A before `--role implementer` (reuse pack `--owns-requirement` / `--tokens` refuse / reserved `RUNTIME_OWNERSHIP` / no `of merge`; never invent a consumer **Sí**); Living map Prod§15 teaches a runbook path in `done_when` / close refuse (`SkillRunbookPath`); `/of` points at the sibling plus the appendix; `install.sh` copies the appendix with the skill tree; `SkillSurface` + `validate-skill.sh` cap the core and require appendix markers; claims honesty scans the appendix; not a second skill / new CLI / supervisor / token ceiling | CHANGELOG / SKILL / alias / AGENTS / kernel feature / roadmap | `SkillSurface`; `SkillSurfaceCore`; `SkillProductionMode` | `SKILL.md` / `references/skill-appendix.md` / `of/SKILL.md` / `scripts/skill_surface.py` / `scripts/validate-skill.sh` / `tests/test_packaging.py` | `SkillSurface` / `SkillSurfaceCore` | — | critical | OK | 0.7.79; Prod§15 0.7.89 |
| C-151 | Before claiming shipped / closed / done on a field, the leader must run `of contrast` and `of close --checklist` in the same turn and quote `contrast RESOLVED` + `residual empty` + the printed `speak` line (`do not claim shipped unless contrast RESOLVED and residual empty`); `of close --checklist` prints a reused `CloseChecklist.speak_line()` after the existing contrast + residual rows (human only; machine JSON unchanged); if contrast is not RESOLVED or residual is MISSING or that `speak` line is not quoted the leader may not claim shipped (protocol, not a kernel chat parser; the evaluator `speak` row is a different line); pairs with 0.7.58 quote-PULSE (`InFlightSignal.speak_line`); SKILL core + `/of` + appendix teach the duty; no new CLI / schema / event / supervisor / Stop hook | CHANGELOG / SKILL / alias / appendix / kernel feature / roadmap | `CloseChecklist.speak_line`; `SkillAntiDoneTheater`; `CloseChecklistProof` | `scripts/of/cli/spec_cmd.py` / `tests/test_kernel_spec.py` / `tests/test_packaging.py` / `SKILL.md` / `of/SKILL.md` / `references/skill-appendix.md` | `CloseChecklist` / `cmd_close` | — | critical | OK | 0.7.68; speak-quote 0.7.87 |
| C-152 | `of status` / `of resume` derive `spawned` counts and ALIVE/QUIET/STALE-style verdicts from spawn metadata (`waves/<n>/spawns/<id>.json`) or the same scratch evidence `of pulse` uses — not packet presence alone; a packed child with no spawn record and no scratch activity reads `PACKED` / `spawned 0` and `next SPAWN` (`of spawn` / `of handoff`), not ALIVE or HOLD-as-if-running; a started-only spawn (`SpawnRecord.unsettled` / `flying`) dominates a leftover residual so in-flight stays honest; a settled spawn without a schema-valid residual reads `done_without_residual` (not `ok` / not ALIVE from leftover PULSE); `children_spawned` pack budget is unchanged; SKILL / `/of` / appendix teach the wording; no new CLI / schema / supervisor / token ceiling | CHANGELOG / SKILL / alias / appendix / kernel feature / roadmap | `SpawnRecord`; `PackedOnlyNotAlive`; `SkillPackedOnlyStatus`; `SkillRespawnInFlight`; `SpawnEndedWithoutResidual` | `scripts/of/field.py` / `scripts/of/cli/ops.py` / `tests/test_kernel_field.py` / `SKILL.md` / `of/SKILL.md` | `SpawnRecord` / `child_pulse_verdict` / `StatusReport` / `next_legal_action` | — | critical | OK | 0.7.70; leftover residual 0.7.91; ended-without-residual 0.8.7 |
| C-153 | Public docs do not claim external multi-agent field dogfood. Re-runnable proof today is in-repo lab: unittest, `of eval --strict --kernel` (recovery evals), mortal-install (C-127), claims-matrix / `check-claims.py`, and the external-brief threat table. External field dogfood remains Partial — no invented customers, fields, or outcomes. SKILL / `/of` / appendix / README / external-brief teach the same sentence. `FieldEvidenceHonesty` locks it. No new CLI / schema / supervisor / case study | SKILL / alias / appendix / README / external-brief / evals / AGENTS | `FieldEvidenceHonesty`; recovery evals; `MortalInstallDemo`; `ClaimsHonesty` | `SKILL.md` / `of/SKILL.md` / `references/skill-appendix.md` / `README.md` / `docs/external-brief.md` / `tests/test_packaging.py` | `FieldEvidenceHonesty` | — | normal | Partial | keep Partial until a checkable external field artifact is cited |

### Verdict definitions

| Verdict | Meaning |
|---------|---------|
| **OK** | Matches code |
| **Partial** | Exists but incomplete vs claim |
| **Missing** | Not found in code |
| **Contradicted** | Code conflicts with claim |
| **Unverifiable** | Not a structural claim |

### Truth score (advisory) vs CI

```text
score = (OK_N * 100 + PARTIAL_N * 50) / TOTAL_V   # TOTAL_V > 0
```

If any **critical Contradicted** exists, CI **must** fail. Advertised truth score must match the table and stay ≤98%. Marketing theater on SKILL / `/of` / README fails. `python3 docs/audit/check-claims.py` is wired into `scripts/validate-skill.sh`.

## Follow-on plan

- [x] Patch Contradicted lock/path/location claims in supporting docs
- [x] Add STAR to each supporting markdown doc
- [x] Expand hub coverage + docs table
- [x] Duplicate `ops.py` spec handlers gone on main 0.6.9 (C-055)
- [x] C-070 LEARN-002 OK on 0.7.1; C-080 Partial (no independent review in merge history)
- [x] C-071 OK on 0.7.2: WAL-002 writer rematerialize; immediate checkpoint keeps e1/e2
- [x] C-082 OK on 0.7.2: unpack + complete-stale use `packet_residual_file`
- [x] C-083 OK: #49 `done_when_closed` in integration digest
- [x] C-041 RETAIN-001: `gc` dump is permanent unlink; not a restorable dump
- [x] C-084 SAT-001..006 0.7.3: walk every home, 7-day safe TTL, HITL drop/keep
- [x] C-085..C-088 0.7.4: #54 pack continuation, #57 integrate JSON stdout, #55 hyphen PREFIX message, #56 skip-warn throttle
- [x] C-089 0.7.5: invariant evals (mission rewrite / contract close / slogan) + external brief
- [x] C-090 0.7.6: pack exclusivity eval + threat-model section (child-cannot vs kernel-does-not-stop)
- [x] C-091..C-093 0.7.7: atomic close, ACTIVE pointer, done_when lint
- [x] C-094..C-096 0.7.9: stale-field abandoned signal, skip-explore theater honesty, multi-harness residual contract
- [x] C-097 0.7.10: doctor skill VERSION skew; close-honesty + nested-fields guides
- [x] C-098 0.7.11: ArgvRedact keeps schema basename on a deep skill root
- [x] C-099 0.7.12: durable multi-day resume; unique-field later session; re-init refuse
- [x] C-100 0.7.13: sibling FieldRoster ACTIVE marker + packed-age + epic vs patch choose
- [x] C-101 0.7.15: budget.seconds honesty; spawn --timeout must match or omit
- [x] C-102 0.7.16: mid-flight amend+patch; next packet carries dated amend
- [x] C-103 0.7.17: wave-report quality gate; chat-dump residual cannot collect
- [x] C-104 0.7.18: packet sizing lint; whole-phase slogans die; ≥800 stays advisory
- [x] C-105 0.7.19: threshold stop-spawn loop; pack/spawn blocked until patch+next-wave
- [x] C-106 0.7.20: resume after process death; live wave from disk; no re-init theater
- [x] C-107 0.7.21: stronger generic done_when lint; empty theater closes refused
- [x] C-108 0.7.22: in-flight packed-age watchdog; status/resume name packs past 7d SLA
- [x] C-109 0.7.23: contrast report renderer; one-pager + machine JSON; no dual-truth
- [x] C-110 0.7.24: `of wave list` / `of wave show`; live wave marked; no second ledger
- [x] C-111 0.7.25: doctor one-pass skew (ACTIVE + version + stale packs)
- [x] C-112 0.7.26: orphan packed-child cleanup; explicit gc with stamp proof; auto-gc skips packets
- [x] C-113 0.7.27: root stub vs nested fields; refuse `--field` / migrate archive; no silent delete
- [x] C-114 0.7.28: `of status --json` live-wave dashboard document; `StatusReport`; no second ledger
- [x] C-115 0.7.29: 3-wave residual-loop eval corpus; mid-flight amend lands on later packets
- [x] C-116 0.7.30: nested field lifecycle; `of new --parent` / close returns ACTIVE; not `of merge`
- [x] C-117 0.7.31: mid-epic handoff packet; `of handoff` without `--packet`; no unpack
- [x] C-118 0.7.32: closed-field archive keeps CLOSE.json; drop without --force cannot wipe contrast trail
- [x] C-119 0.7.33: live in-flight visibility; `running` + residual MISSING on status/resume/pulse
- [x] C-119 0.7.91: leftover residual does not hide started-only re-spawn; `SpawnRecord.flying`
- [x] C-120 0.7.34: `of pack --explain` dry-run slice sizing; no write; same `SliceLint`
- [x] 0.7.35: long-task residual-theater threat-model addendum (docs; existing evals)
- [x] 0.7.36: long-mission operator walk (`docs/long-mission.md`; existing verbs + evals)
- [x] C-121 0.7.37: adversarial dual-truth corpus; dual-truth close / fake tokens / unpack theater
- [x] C-122 0.7.38: pack `--out` accepts physical nested path; `PackOutPhysicalNested`
- [x] C-123 0.7.39: doctor skill SKEW advisory; residual awaiting/MISSING; first-home roster `first`
- [x] C-124 0.7.40: live PULSE progress under `running` on status/resume/pulse; HOLD+in-flight one-liner
- [x] C-125 0.7.41: cross-field open-pack roster; `of fields --json`; `PackRoster`
- [x] C-126 0.7.42: multi-wave close checklist; contrast + residual empty; `--checklist` dry-run; flying is not closed
- [x] C-127 0.7.43: mortal-install one-sitting demo; `install.sh` + `of doctor`; disk contract named
- [x] C-128 0.7.44: claude/codex/cursor residual matrix on deep-install dests; `MultiHarnessResidual`
- [x] C-129 0.7.45: close-is-proof RFC invariants; residual empty ≠ closed; SKILL points at the page
- [x] C-130 0.7.46: daily ask-to-update when `of` is behind; consent via `install.sh --from-release` + SHA256SUMS; not silent
- [x] C-131 0.7.47: optional per-task model hints; consent + spawn `--model` passthrough; no silent switch
- [x] C-132 0.7.48: efficiency signal + propose uptier/downtier; residual.usage provenance; ask only; no fake budget.tokens
- [x] C-133 0.7.49: `of contrast --diff` narrative; same ContrastReport + spec-diff facts; no theater; RESOLVED is not CLOSED
- [x] C-134 0.7.50: claims honesty gate; advertised score ≤98% and matches table; theater on SKILL / `/of` / README dies; `check-claims.py` in `validate-skill.sh`
- [x] C-135 0.7.51: README use-case opening before kernel abstractions; Haken analogy + principles link below; `ReadmeProductSurface`
- [x] C-135 0.7.96: authority-axis hero + planning-with-files contrast + `./install.sh` → first close; SKILL/`/of`/appendix teach the same framing; no new C-ID (honesty cap)
- [x] C-136 0.7.52: README Mid-flight H2 before Install; three kinds of change + sibling fields intact; `ReadmeProductSurface`
- [x] C-137 0.7.53: skill-first cheap vs frontier propose in chat before multi-role pack; consent; `SkillLeaderInitiative`
- [x] C-138 0.7.54: quoted skill YAML so agy discovers the skill; Shared `~/.gemini/skills`; `SkillFrontmatterQuotedGate`
- [x] C-139 0.7.55: residual optional top-level `v`; collect ignores kernel `--json` document version; Codex derivative lockstep
- [x] C-140 0.7.56: ResidualQuality byte cap skips honest structured evidence; transcript dumps still refuse; size-refuse names notes.md + re-spawn
- [x] C-141 0.7.57: grok + agy consented `--model` passthrough; named model; flags before `-p`; no invented aliases
- [x] C-142 0.7.58: turn-end quote-PULSE leader duty; `InFlightSignal.speak_line` on status/resume; reuses 0.7.40; no manual `of pulse`
- [x] C-143 0.7.59: skill-first ask same-harness categories vs multi-harness mix before pack; consent; `SkillHarnessAsk`; C-014 stays Partial
- [x] C-143 0.7.83: multi-harness mix playbook (when mix vs roles on one harness; claude/codex/cursor/grok/agy; bind pack/spawn/collect/contrast/close/doctor + detect consent); `SkillHarnessMix`; no new C-ID (honesty cap)
- [x] C-132/C-143 0.7.84: long-task efficiency mix from honest signals only; `AdapterBalance` unknown unless published payload in hand; never invent; `SkillEfficiencyMix`; no new C-ID (honesty cap)
- [x] C-080 0.7.85: skill/kernel teach a fresh-context review packet (`adversary`/`verifier`) before close; ask consent; never silent; not a close gate; not `of merge`; C-080 stays Partial (honesty cap)
- [x] C-045 0.7.86: Orca `worker-start` ↔ `worker-stop` then `worker-release` after collect/abandon; leftover of-worktree doctor WARN; no process supervisor; `SkillOrcaWorkerTeardown` / `DoctorWorktreeLeftover`; no new C-ID (honesty cap)
- [x] C-045 0.7.90: native Codex spawn binds recorded worktree + field/Git writable roots or refuses before launch; `CodexRecordedWorktree` / `SkillCodexWorktreeSpawn`; no new C-ID (honesty cap)
- [x] C-009/C-015 0.7.60: `AdapterDetect` present / missing / PATH≠auth on detect + doctor; never claim login from PATH; no new C-ID (honesty cap)
- [x] C-144 0.7.61: living model catalog; consult-before-propose; sourced or unknown; not `budget.tokens`; `ModelCatalogHonesty` / `SkillModelCatalogConsult`
- [x] C-145 0.7.62: stream-json / JSON streams feed the same PULSE; `StreamJson` + `PulseProgress.append`; residual extract reused; no invented stream-json; no supervisor
- [x] C-145 0.7.69: Claude `-p` + stream-json also emits `--verbose` (#131); cursor/codex unchanged; no new C-ID (honesty cap)
- [x] C-145 0.7.71: grok documented `--output-format streaming-json` + spawn metadata finalize on exit/timeout/missing binary (#133); `GrokAdapterSpawn`; no new C-ID (honesty cap)
- [x] 0.7.72: `of issue --body-file` refuse names `.orderfield/work/scratch/<child_id>/`; leader HITL draft `…/leader/ISSUE.md`; `IssueCli` / `SkillIssueBodyFileLeader`; no new C-ID (honesty cap)
- [x] C-027 0.7.73: Codex `residual.codex.schema.json` `usage.type` is `["object","null"]` once; unique type unions; `#135`; no new C-ID (honesty cap)
- [x] C-111 0.7.74: closed-field historical packs are informational (field id + wave; not FAIL); open-field pack SKEW still FAIL; `#137`; no new C-ID (honesty cap)
- [x] C-146 0.7.75: collect missing-residual conservative diagnostic is factual (pending/unavailable; adapter-tied possibility, not cannot-write); `#138`; no new C-ID (honesty cap)
- [x] 0.7.76: `integrate --partial` hold reason names `skipped_in_flight` instead of `wave closed` while siblings fly; `#139`; `hold_if_partial_in_flight`; no new C-ID (honesty cap)
- [x] C-056 0.7.77: `of learn` length is advisory (`LearningLint`; store + note over 400 chars); `LEARNING_MAX_LINES` stays hard; `#146`; no new C-ID (honesty cap)
- [x] C-047 0.7.78: webhook HMAC + replay is a contrast PAIR gate (`WebhookPair`; accept + reject; `--both-sides`); not a webhook server; no new C-ID (honesty cap)
- [x] C-150 0.7.79: appendix **Production mode** + Gate A before `--role implementer`; `SkillSurface` markers; `SkillProductionMode`; no `of gate`; no new C-ID (honesty cap)
- [x] C-047 0.7.80: timeout / idempotency / health are VERIFIED_CONTRACT (`ContractSurface`; extract TIMEOUT/HEALTH; `--surface internal` cannot hide them); no new C-ID (honesty cap)
- [x] C-033/C-051 0.7.81: done close evidence requires artifact SHA of `result_ref` + rollback command (`CloseEvidence`); captions die; no new C-ID (honesty cap)
- [x] C-146 0.7.63: conservative agy `denied_actions` copied into residual; missing/empty is not approval; `yolo` does not copy; `AgyDeniedActions`
- [x] C-147 0.7.64: agy `--json-schema` residual reuses Codex file; Claude omit (inline-only / keep stream-json); Qwen omit; `OutputSchema`
- [x] C-148 0.7.65: `OF_TRUST=plan` maps cursor/agy `--mode plan` + grok `--sandbox read-only`; honesty omit for gated/missing modes; `TrustNativeFlags`
- [x] C-149 0.7.66: adapter resume/continue only when residual already has session id; never invent; never `--continue`; `AdapterResume`
- [x] C-150 0.7.67: SKILL.md short core + `references/skill-appendix.md`; `SkillSurface`; hosts load the core; 100% verb coverage stays
- [x] C-151 0.7.68: mechanical anti-done-theater; `CloseChecklist.speak_line` on `--checklist`; SKILL teaches contrast RESOLVED + residual empty before claiming shipped; pair with quote-PULSE
- [x] C-151 0.7.87: claiming shipped also requires quoting the printed `speak` line (`CloseChecklist.SPEAK`); `SkillAntiDoneTheater` refuses pages that only say checklist prints speak; no new C-ID (honesty cap)
- [x] C-047 0.7.88: `/version` or a release header is VERIFIED_CONTRACT (`ContractSurface`; extract VERSION; `--surface internal` cannot hide); no new C-ID (honesty cap)
- [x] C-093/C-150 0.7.89: Prod§15 day-90 runbook path in `done_when` before close (`RunbookPath`; production/day-90 cue only; `SkillRunbookPath`); no new C-ID (honesty cap)
- [x] C-119/C-152 0.7.91: started-only re-spawn dominates a leftover residual (`SpawnRecord.flying`); status/resume/pulse stay `running` + speak; `SkillRespawnInFlight`; no new C-ID (honesty cap)
- [x] C-083 0.7.92: successful `of phase` refreshes the covering digest (`PhaseDigest`) so `of next-wave` does not require `--recompute`; `#49` stays; `SkillPhaseNextWave`; no new C-ID (honesty cap)
- [x] 0.7.93: generic `OF_AGENT` is shell-quoted argv (`GenericAgent` / `shlex.split`); dry-run prints `shlex.join`; `SkillGenericAgentArgv`; no new C-ID (honesty cap)
- [x] C-034 0.7.94: empty-wave `of phase` skips integrate when `packed_children` is empty; `done_when_closed` / in-flight stay; `#166`; `SkillEmptyWavePhase`; no new C-ID (honesty cap)
- [x] C-034/C-035 0.7.95: resume names `INTEGRATE --RECOMPUTE` on digest drift; `IntegrationDigest` omits spawn-owned residual keys; `#168`; `SkillResumeRecompute`; no new C-ID (honesty cap)
- [x] 0.7.97: one VERSION / GitHub tag per proven invariant (`PackagingBump`; current heading needs `**Proof:**`; docs-only / unproven fail; anti 10-tags/day); `PackagingBumpDiscipline`; no new C-ID (honesty cap)
- [x] 0.7.98: identity-stale + flying prints `UNPACK --FORCE` not spawn; `of spec --add` / `--amend` warns that the rev bump stales N packet(s) (`PacketRevStale`; #178); `RevStaleDeadChild` / `SkillRevStaleUnpack`; no new C-ID (honesty cap)
- [x] C-091 0.7.99: successful close is terminal (`CloseProof` + `ActiveField.release_closed` + pulse/status/doctor); not ACTIVE / not ALIVE / `spawn_blocked` cleared; `recovery/post-close-terminal`; no new C-ID (honesty cap)
- [x] C-084/C-111 0.7.100: doctor / close WARN when audit is OVER or scratch is fat (`AuditPressure`; #181); not FAIL; not a close gate; `DoctorAuditPressure` / `SkillAuditPressure`; no new C-ID (honesty cap)
- [x] C-111 0.7.101: leftover root ORDER.json SKEW names `migrate required` (FAIL); sibling fields without CLOSE are hygiene WARN; `#182`; `DoctorOnePassSkew` / `SkillDoctorOpenHygiene`; no new C-ID (honesty cap)
- [x] C-111 0.8.1: cited plan docs stale vs last integrate print `docs_sync` WARN (`PlanDocSync`; #188); Mode A patch or Mode B dump+ask; not a close gate; `DoctorPlanDocSync` / `SkillPlanDocSync` / `recovery/plan-doc-sync`; no new C-ID (honesty cap)
- [x] C-025 0.8.2: after collect+integrate, execute printed `next` same turn (`DriveAfterIntegrate`; report is not a stop; ordinary next-wave is not a consent ask; #191); `DriveAfterIntegrateProof` / `SkillDriveAfterIntegrate` / `recovery/drive-after-integrate`; no new C-ID (honesty cap)
- [x] 0.8.3: `of issue` create requires `--confirm` or TTY yes (`IssueConfirm`; dry-run is not HITL; #193); `IssueConfirmLock` / `SkillIssueConfirm`; no new C-ID (honesty cap)
- [x] C-066/C-067 0.8.4: `OF_TRUST=yolo` + `OF_SPAWN_ENV=inherit` are audited operator actions (`OperatorAction`; speak + `operator_actions` on spawn meta/events; not silent defaults); `OperatorActionAudit` / `SkillOperatorAction`; no new C-ID (honesty cap)
- [x] 0.8.5: `of issue --search` lists open issues via Issues list API + local filter (`IssueList`; #198); empty/omitted = all; `gh --search` is not used; `IssueSearchList` / `SkillIssueSearch`; no new C-ID (honesty cap)
- [x] C-131 0.8.6: Cursor `--model-tier` without `--model` refuses pack/spawn (`AdapterHints.TIER_NEED_MODEL`; no invented frontier alias; #199); `AdapterHintsCli` / `SkillCursorTierRefuse` / `recovery/cursor-tier-model`; no new C-ID (honesty cap)
- [x] C-028/C-152 0.8.7: ended spawn without a schema-valid residual is `done_without_residual` not ok/ALIVE; host Write denials are not `escalate_up` (`HostWriteDenial`; #200); `SpawnEndedWithoutResidual` / `SkillSpawnEndedSignal` / `recovery/spawn-ended-without-residual`; no new C-ID (honesty cap)
- [x] 0.8.8: `of issue --body-file` accepts leader `ISSUE.md` / `ISSUE-*.md` under `.orderfield/work/scratch/leader/` (`_issue_scratch_rel_ok`; #201); child `ISSUE.md` / `issues/<slug>.md` stay; `IssueCli` / `SkillIssueBodyFileLeader`; no new C-ID (honesty cap)
- [x] 0.8.9: after successful `of collect` (`ok=N invalid=0 missing=0`), resume/status `next` is INTEGRATE not COLLECT (`CollectReady`; #204); `ResumeAfterIntegrate.test_next_is_integrate_after_successful_collect` / `SkillCollectNextIntegrate`; no new C-ID (honesty cap)
- [x] 0.8.10: `of gc` / `of retain` import `PHASES` so a stored learning with `phase` does not NameError (`#205`); `LearningApplicablePhase` / `EpisodicRetention.test_gc_and_retain_survive_field_learning_with_phase`; no new C-ID (honesty cap)
- [x] C-045 0.8.11: after collect/abandon, `of worktree remove --child-id` plus Orca Host `worktree rm` / `terminal close --tab`; doctor/close WARN on orphaned vs settled (`DoctorSkew.orphaned_worktrees`; #210); `DoctorWorktreeLeftover` / `SkillOrcaWorkerTeardown`; no new C-ID (honesty cap)
- [x] C-025 docs honesty: clone/checkout of open `.orderfield/` + installed skill auto-continues; operator risk, not an escape; rule 0 stays; `SkillCheckoutAutoContinueHonesty`; no new C-ID; no VERSION bump
- [x] C-152 0.7.70: packed-only is not ALIVE; `SpawnRecord` + status/resume `spawned` from spawn meta; `PACKED` + `next SPAWN`; pulse stays authoritative
- [x] C-153 docs honesty: in-repo lab proof is re-runnable; external multi-agent field dogfood stays Partial; `FieldEvidenceHonesty`; no invented case studies; no VERSION bump
- [x] C-015/C-016 docs honesty: detect ≠ credentials/session authority; worktree/process bounds are honesty surfaces, not a security guarantee; `HardnessDetectAuthWorktree`; stay Partial; no new C-ID; no VERSION bump
- [x] Duplicate C-065 retired (shim → C-081); uniqueness gate `docs/audit/check-claims.py`
- [x] Wire `docs/audit/check-claims.py` into `validate-skill.sh` (0.7.50 honesty gate)
- [ ] Optional: wire consumer `audit-claims.sh` if this package wants a docs CI gate beyond `validate-skill.sh`

## Related

- Hub: [AGENTS.md](../../AGENTS.md)
- Architecture: [architecture.md](../architecture.md)
- Out of scope: [out-of-scope.md](out-of-scope.md)
- Recovery reports: [README.md](README.md)
- Uniqueness: `python3 docs/audit/check-claims.py`
