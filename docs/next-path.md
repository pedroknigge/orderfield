# Next path (after 0.7.67)

`SKILL.md` is a short always-loaded core. The rest of the leader procedure is `references/skill-appendix.md`. Hosts load the core. Agents read the appendix before pack / spawn / contrast / close. `SkillSurface` is the gate. `AdapterResume` emits `--resume ID` only when `residual.session_id` is already set (claude/cursor). Cold residual is a fresh spawn. Never invent. Never `--continue`. `OF_TRUST=plan` maps cursor `--mode plan`, agy `--mode plan`, grok `--sandbox read-only`. Claude `auto` stays `acceptEdits`. Cursor/OpenCode/Grok `auto-edit`/`auto` stay conservative. agy spawn `--json-schema` reuses `residual.codex.schema.json` (`OutputSchema`). Claude omit: `--json-schema` is inline-only and would drop stream-json PULSE. Qwen omit: structured_output tool, not residual delivery. Conservative agy spawn copies nonempty harness `denied_actions` into optional `residual.denied_actions` (`AgyDeniedActions`). Missing/empty is not approval. `yolo` does not copy. The living model catalog (`docs/model-catalog.md` + `.json`) is advisory for the skill/leader. Consult before cheap/frontier or mix. Cite public sheets or mark unknown. Not a router. Not `budget.tokens`. Grok and agy honor consented `--model` passthrough (named model; flags before `-p`; no invented cheap/frontier aliases). `ResidualQuality` accepts honest structured evidence over 4000 chars (counts, paths, shas). Transcript dumps still cannot collect. Residual collect accepts optional top-level `v` (kernel `--json` document version; ignored). README leads with typical problems → what Orderfield does, including a consent cheap vs frontier propose. Mid-flight plan-change H2 sits before Install. The `/of` skill must propose that split in chat before a multi-role pack. Published SKILL / `/of` / README claims stay ≤98% honest (`python3 docs/audit/check-claims.py`). `of contrast --diff` narrates SPEC vs coverage from `ContrastReport` + `spec-diff`. RESOLVED is not CLOSED. The efficiency signal scores landed residuals (quality × optional `residual.usage`) and may propose a model-tier ask. Hints stay on 0.7.47 `AdapterHints`. `budget.tokens` stays reserved. Close-is-proof RFC names what closed means: contrast RESOLVED + residual empty + `CLOSE.json`. Residual empty is the end of flying, not the close. Claude / Codex / Cursor residual dry-run stays green from deep-install skill dests (`~/.claude` / `~/.agents` / `~/.cursor`). Codex `--output-schema` still names `residual.codex.schema.json`. The mortal-install one-sitting demo wraps `install.sh` + `of doctor` and names the disk contract. `of close --checklist` prints contrast + residual empty and does not stamp. Flying (residual MISSING) cannot close. `of fields` lists open packs across sibling homes; `of fields --json` is the epic-dashboard object. Live `PULSE` progress lines print under `running` on status/resume/pulse. Spawn stream-json / JSON streams (claude/cursor/codex) feed that same file. Leader quotes one line each HOLD+in-flight turn. Doctor skill SKEW is advisory (`doctor WARN` / exit 0). Pack/handoff name residual dest as awaiting/MISSING. First-home roster label is `first`. Pack `--out` still accepts the physical nested path. The adversarial dual-truth corpus stays on disk. The long-mission operator walk and the 0.7.35 residual-theater addendum stay the prose pages. `of pack --explain` stays proven. Live in-flight visibility, closed-field archive, mid-epic handoff, nested field lifecycle, the 3-wave residual loop, `of status --json`, root stub vs nested fields, orphan packed-child cleanup, doctor one-pass skew, wave list/show, contrast one-pager, packed-age watchdog, stronger done_when lint, resume after process death, threshold stop-spawn, packet sizing lint, wave-report quality, mid-flight amend, and `budget.seconds` honesty stay on the published line.

No process supervisor, no bot org, no `of merge`. No fake token budgets.

Still later (not this release): Grok Bot contrast stays written — stay-on-the-run + that table. Do not clone a bot org. `RUNTIME_OWNERSHIP` stays reserved. Brief: [external-brief.md](external-brief.md) · Contrast: [roadmap.md](roadmap.md#grok-bot-contrast-protocol-pick-not-a-bot-org).

REVIEW-001 (an independent approving review in merge history) is still unproven on a solo-collaborator repo. That is a human merge habit. It is not `of merge`.
