# Next path (after 0.8.5)

`of issue --search [QUERY]` lists open issues via the Issues list API plus a local filter (`IssueList`; empty/omitted = all; `#198`). `gh --search` is not used. `OF_TRUST=yolo` and `OF_SPAWN_ENV=inherit` are audited operator actions (`OperatorAction`): spawn speaks, records `operator_actions`, and the skill must ask first. Conservative + allowlist stay quiet defaults. Not a TTY lock. Not a new CLI. Mutating `of issue` create requires `--confirm` after human yes, or a TTY yes (`IssueConfirm`). `--dry-run` previews argv and is not HITL. `--search` stays a read. After collect+integrate, idle + actionable `next` prints `DriveAfterIntegrate.speak` (report is not a stop; execute printed next this turn). Ordinary next-wave/pack is not a consent ask. Cited plan docs (`docs/plans/…`) that the field read stay living: Mode A patch them or Mode B dump `work/scratch/leader/DOCS_SYNC.md` and ask to promote (`PlanDocSync`; `docs_sync stale|pending|findings`). WARN, not FAIL, not a close gate. `of doctor` / `of close` WARN when tree audit is OVER or scratch is fat (`AuditPressure`); `of gc --audit` then shrink before close. Not FAIL. Not a close gate. `of doctor` names leftover root ORDER.json `migrate required` (FAIL) and two or more sibling fields open without CLOSE (hygiene WARN). Successful close is terminal (`CloseProof` / `ActiveField.release_closed`): the finished field is not ACTIVE, `of pulse` is not ALIVE, `spawn_blocked` clears, status/doctor treat the home as closed (`recovery/post-close-terminal`; #180). One VERSION and one GitHub release tag per proven invariant (`PackagingBump`; current CHANGELOG heading names `**Proof:**`; docs-only / unproven current cuts fail; anti-pattern: 10-tags/day). README/SKILL sell on the authority axis (anyone can persist a plan; only the leader may change it). Install leads with `./install.sh` + `of doctor`; first close reuses the 30-second loop. Compared-to names planning-with-files (same disk-plan category, different product). No markdown+hooks catch-up. Identity-stale + flying prints `UNPACK --FORCE` (do not spawn); `of spec --add` / `--amend` warns that the rev bump stales N packet(s) (#178). Resume prints `INTEGRATE --RECOMPUTE` when the wave report digest drifted; `IntegrationDigest` omits spawn-owned `session_id` / `denied_actions` so post-integrate spawn finalization does not deadlock next-wave (#168). Empty-wave `of phase` skips integrate when `packed_children` is empty (#166). `done_when_closed` and in-flight stay. `--force` is not the skip-cut path. Generic `OF_AGENT` is a shell-quoted argv (`GenericAgent` / `shlex.split`). Dry-run prints `shlex.join` of the real list so a space path stays one token. A successful `of phase` refreshes the just-integrated wave covering digest (`PhaseDigest`) so `of next-wave` does not require `--recompute` (#164). `#49` `done_when_closed` stays in the digest. Started-only re-spawn dominates a leftover residual: `of status` / `resume` / `pulse` stay `running` + PULSE + speak until that spawn settles (`SpawnRecord.flying`). A prior collect-refuse file does not hide flight. No archive path. No supervisor. Native Codex spawn honors a packet child's recorded `of worktree` with `-C <worktree>`, field-home `--add-dir`, and exact Git-common-dir `--add-dir`; invalid records refuse before launch. Prod§15 day-90 ops: `done_when` must name a repo-relative runbook path before `of close` can stamp (`RunbookPath` on `DoneWhenLint.refuse_close`). Applies when SPEC / ORDER already name day-90 / Prod§15 / runbook. Toy fields stay. Theater placeholders stay `DoneWhenLint`. Not an on-call bot. No `of gate`. `/version` or a release header SPEC IDs are public-surface VERIFIED_CONTRACT (`ContractSurface`). Extract names `VERSION-`. `--surface internal` cannot hide them. Same close gate as `/health`. Not CloseEvidence SHA+rollback. Not a version server. No `of gate`. Orca interactive `worker-start` must pair `worker-stop` then `worker-release` after collect or abandon. Default is release. `of worktree remove` if add was used. `of doctor` warns on leftover recorded of-worktrees only — no Orca process poll. Not a supervisor. After a wave, SKILL / `/of` / appendix teach a fresh-context review packet (`adversary` / `verifier`) before `of close`: **must ask** consent, never silent. `EvaluatorPacket` reuses `WaveRoster`. Not a close gate. C-080 stays Partial. Not `of merge`. SKILL / `/of` / appendix **Long-task efficiency mix** teach mid-mission cheap/frontier + harness rebalance from honest signals only (`EfficiencySignal`, residual quality, `of detect`, `AdapterBalance`). No published balance/session probe → unknown; never invent. `budget.tokens` stays reserved. Ask before any switch. SKILL / `/of` / appendix **Multi-harness mix** teach when to mix harnesses vs roles on one harness (claude / codex / cursor / grok / agy) and bind mix language to `of pack` / `of spawn` / `of collect` / `of contrast` / `of close` / `of doctor` + `of detect` consent (PATH≠auth). Captions-only mix pages fail `SkillHarnessMix`. Living product/docs map: checklist → of contrast / of close / residual. Not a second checklist. `status=done` close evidence requires `artifact_sha` (sha256 of `result_ref`) and `rollback:` a command (`CloseEvidence`). Captions and mismatched hashes cannot collect. Timeout / idempotency / health / version SPEC IDs are public-surface VERIFIED_CONTRACT (`ContractSurface`). Extract names `TIMEOUT-` / `HEALTH-` / `VERSION-`. `--surface internal` cannot hide them. Idempotency stays PAIR. `/version` or a release header is the same shape as `/health`. Not a health monitor. Not a timeout supervisor. Not a version server. No `of gate`. Skill appendix **Production mode** teaches production-mode invariants and Gate A before `--role implementer`. Use the full verb table. Do not invent a supervisor, bot org, `RUNTIME_OWNERSHIP`, `--tokens`, or `of merge`. No `of gate`. Webhook HMAC signature + replay is a contrast PAIR gate (`WebhookPair`): accept a valid signature and reject replay or a bad signature, then `of spec --verified-contract ID --both-sides`. Not a webhook server. Not integration replay. `of learn` length is advisory like `pack --slice`: over 400 chars still stores and prints a note; over 4 lines still refuse dumps. `of integrate --partial` keeps `hold` when landed residuals are complete and siblings remain in flight; the reason names `skipped_in_flight` and does not say the wave closed. `of collect` reports a missing residual as pending/unavailable with known adapter / trust / outcome and actual `denied_actions`; conservative headless permissions are an adapter-tied possibility, not a cannot-write claim. `of doctor` treats closed-field historical packs as informational (field id + wave; not FAIL). Open-field stale/aged packs still FAIL. Do not rewrite a closed audit trail. Codex `--output-schema` accepts `residual.codex.schema.json` because `usage.type` is `["object","null"]` once (no duplicate null). `of issue --body-file` refuses non-canonical paths and names `.orderfield/work/scratch/<child_id>/`. A leader HITL draft is `.orderfield/work/scratch/leader/ISSUE.md` (or the child's existing `ISSUE.md` / `issues/<slug>.md`). Grok spawn uses documented `--output-format streaming-json` so residual extract and the same scratch PULSE reuse the claude/cursor path. Spawn metadata is finalized on exit, timeout, and missing binary even when a grandchild keeps stdout open. Packed-only children read `PACKED` / `spawned 0` / `next SPAWN` — not ALIVE or HOLD-as-if-running. `SpawnRecord` reuses spawn metadata; pulse stays the activity lens. Claude spawn argv includes `--verbose` with `-p` + stream-json so Claude Code accepts the stream. Cursor/codex unchanged. Claim shipped is mechanical: `of close --checklist` prints `CloseChecklist.speak_line()`; quote the printed `speak` line plus `contrast RESOLVED` + `residual empty` or do not claim shipped. Pair with quote-PULSE while flying. `SKILL.md` is a short always-loaded core. The rest of the leader procedure is `references/skill-appendix.md`. Hosts load the core. Agents read the appendix before pack / spawn / contrast / close. `SkillSurface` is the gate. `AdapterResume` emits `--resume ID` only when `residual.session_id` is already set (claude/cursor). Cold residual is a fresh spawn. Never invent. Never `--continue`. `OF_TRUST=plan` maps cursor `--mode plan`, agy `--mode plan`, grok `--sandbox read-only`. Claude `auto` stays `acceptEdits`. Cursor/OpenCode/Grok `auto-edit`/`auto` stay conservative. agy spawn `--json-schema` reuses `residual.codex.schema.json` (`OutputSchema`). Claude omit: `--json-schema` is inline-only and would drop stream-json PULSE. Qwen omit: structured_output tool, not residual delivery. Conservative agy spawn copies nonempty harness `denied_actions` into optional `residual.denied_actions` (`AgyDeniedActions`). Missing/empty is not approval. `yolo` does not copy. The living model catalog (`docs/model-catalog.md` + `.json`) is advisory for the skill/leader. Consult before cheap/frontier or mix. Cite public sheets or mark unknown. Not a router. Not `budget.tokens`. Grok and agy honor consented `--model` passthrough (named model; flags before `-p`; no invented cheap/frontier aliases). `ResidualQuality` accepts honest structured evidence over 4000 chars (counts, paths, shas). Transcript dumps still cannot collect. Residual collect accepts optional top-level `v` (kernel `--json` document version; ignored). README leads with typical problems → what Orderfield does, including a consent cheap vs frontier propose. Mid-flight plan-change H2 sits before Install. The `/of` skill must propose that split in chat before a multi-role pack. Published SKILL / `/of` / README claims stay ≤98% honest (`python3 docs/audit/check-claims.py`). `of contrast --diff` narrates SPEC vs coverage from `ContrastReport` + `spec-diff`. RESOLVED is not CLOSED. The efficiency signal scores landed residuals (quality × optional `residual.usage`) and may propose a model-tier ask. Hints stay on 0.7.47 `AdapterHints`. `budget.tokens` stays reserved. Close-is-proof RFC names what closed means: contrast RESOLVED + residual empty + `CLOSE.json`. Residual empty is the end of flying, not the close. Claude / Codex / Cursor residual dry-run stays green from deep-install skill dests (`~/.claude` / `~/.agents` / `~/.cursor`). Codex `--output-schema` still names `residual.codex.schema.json`. The mortal-install one-sitting demo wraps `install.sh` + `of doctor` and names the disk contract. `of close --checklist` prints contrast + residual empty and does not stamp. Flying (residual MISSING) cannot close. `of fields` lists open packs across sibling homes; `of fields --json` is the epic-dashboard object. Live `PULSE` progress lines print under `running` on status/resume/pulse. Spawn stream-json / JSON streams (claude/cursor/codex) feed that same file. Leader quotes one line each HOLD+in-flight turn. Doctor skill SKEW is advisory (`doctor WARN` / exit 0). Pack/handoff name residual dest as awaiting/MISSING. First-home roster label is `first`. Pack `--out` still accepts the physical nested path. The adversarial dual-truth corpus stays on disk. The long-mission operator walk and the 0.7.35 residual-theater addendum stay the prose pages. `of pack --explain` stays proven. Live in-flight visibility, closed-field archive, mid-epic handoff, nested field lifecycle, the 3-wave residual loop, `of status --json`, root stub vs nested fields, orphan packed-child cleanup, doctor one-pass skew, wave list/show, contrast one-pager, packed-age watchdog, stronger done_when lint, resume after process death, threshold stop-spawn, packet sizing lint, wave-report quality, mid-flight amend, and `budget.seconds` honesty stay on the published line.

No process supervisor, no bot org, no `of merge`. No fake token budgets.

Still later (not this release): Grok Bot contrast stays written — stay-on-the-run + that table. Do not clone a bot org. `RUNTIME_OWNERSHIP` stays reserved. Brief: [external-brief.md](external-brief.md) · Contrast: [roadmap.md](roadmap.md#grok-bot-contrast-protocol-pick-not-a-bot-org).

REVIEW-001 (an independent approving review in merge history) is still unproven on a solo-collaborator repo. That is a human merge habit. It is not `of merge`.
