# Next path (after 0.7.4)

Saturation control shipped. Closed sibling fields are visible to `of gc`. Open ORDERs are never auto-deleted; over budget prints `audit` and the human runs `--keep-field` or `--drop-field`.

**Still later (not this release):** Grok Bot engineering-org contrast vs Orderfield. Stay-on-the-run + written contrast. Do not clone a bot org. `RUNTIME_OWNERSHIP` stays reserved. Brief: [roadmap.md](roadmap.md#grok-bot-contrast-protocol-pick-not-a-bot-org).

REVIEW-001 (independent approving review in merge history) remains unproven on a solo-collaborator repo.
