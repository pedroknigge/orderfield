---
name: of
description: v0.7.16 — Alias for the orderfield contract kernel. Use when the user invokes /of or orderfield, an existing field must be resumed, or a genuine multi-slice / multi-writer wave needs a plan that survives compaction. Do not trigger for a harness name alone or one ordinary subagent.
license: MIT
metadata:
  version: "0.7.16"
  alias-of: orderfield
---

# /of — alias for orderfield

Two names. One kernel. `/of` is `/orderfield`.

Load the sibling skill and follow it. Stop if it is missing. Do not invent a second contract.

After package installation, the full skill is normally at
`../orderfield/SKILL.md` relative to this file. In a source checkout, it is at
`../SKILL.md`. If neither path exists, stop and report that the Orderfield skill
package is incomplete.
